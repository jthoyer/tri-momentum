# CLAUDE.md — TRI Momentum

> This file is the project north star. Read it at the start of every session before touching any code.
> Update it when architectural decisions change. Never let it go stale.

---

## ⚠️ Two codebases: current app vs. target architecture

This repo contains **two things at once**, and it matters which one a session is touching:

- **Current app (`index.html`, repo root, plus its two Strength sub-apps)** — `index.html` is a single static HTML/CSS/JS file, no build step, no framework, no accounts; the Strength tab is a core part of the same product, implemented as two co-deployed PWAs (`TRI-swim-strength mobile app/`, `TRI-run-strength mobile app/`) embedded via `<iframe>`. This is **what's actually live** at `https://jthoyer.github.io/tri-momentum/` (GitHub Pages serves the repo root directly) and what recent commits have been iterating on (bottom nav, Strength sub-tabs, week numbering, etc.). Treat all three as the real product — the Strength apps are not an optional add-on.
- **Target architecture (`frontend/`, `backend/`, `supabase/`)** — a React + Vite + Hono + Supabase + Stripe scaffold representing where the product is intended to go: accounts, cross-device sync, a phase-aware tip/content system, and a paywall. It has a single "initial commit" and is **not wired up or in active use** — no `supabase-js` installed, no backend server, `Auth.jsx` isn't reachable from the app, nothing persists past `localStorage`.

Both are intentional. Most of this document (Freemium model, Database schema, Auth flow, Stripe, `tips.js` content system) describes the **target**, not what's running today. Sections are labelled accordingly. When in doubt: if it's not described under "Current implementation" below, assume it doesn't exist yet in the shipped app.

---

## What this product is (north star — target)

**TRI Momentum** is a mobile-first progressive web app (PWA) for intermediate age-group triathletes training for Olympic distance and 70.3 Ironman events. It is not a training log. It is a decision-support tool — it helps athletes make better choices about load, recovery, and race preparation across a repeating training block cycle (base → build → peak → recovery).

The target core value loop is:
1. Athlete sets their training start date and race date
2. App resolves their current phase, block week, and emphasis pillar from those dates
3. App surfaces the right thinking prompt for their current position in the training cycle
4. Athlete logs session outcomes and a weekly reflection
5. App tracks readiness, grey-zone training %, and vulnerable sessions over time
6. Trend data surfaces patterns the athlete can't see week-to-week

The product is built for **one primary user type**: a time-pressured age-grouper with 10–15 hours/week of training, strong self-knowledge, and no interest in being hand-held. The tone of the app — including all copy — reflects this.

**Today's shipped app is a narrower, working subset of this vision** — a reactive check-in log with rule-based pattern detection, no accounts and no coaching-prompt content system (see below). It's the direction, not yet the destination.

---

## Current implementation (`index.html`) — what's actually live

Single self-contained HTML file at the repo root, deployed via GitHub Pages with no build step. No framework, no bundler, no npm dependencies.

### Storage & sync
- **The Google Sheet is the source of truth**, so the log reads the same on the phone and the laptop (no accounts, no login). A personal Sheet, written to and read from via a Google Apps Script Web App (`apps-script/Code.gs`).
  - **Write:** every saved session fires a `no-cors` POST — fire-and-forget, opaque response, a failed write never blocks the check-in (the entry still shows locally this session via the optimistic `log.push` in `save()`).
  - **Read:** the app fetches the full log from the Sheet (`GET .../exec?list=1`, normal `cors` mode since the JSON body needs parsing) on load, and again whenever the This week or Month tab is opened, replacing the in-memory log with whatever the Sheet returns.
  - **`localStorage` is a same-device cache only** — it paints the last-known state instantly on load and is what's used if a Sheet fetch fails (offline, blocked request, extension interference, etc). It is not authoritative and is never the reason two devices would disagree.
- No Supabase, no Postgres, no RLS, no server-side persistence of our own.
- **Race calendar uses a second sheet tab** in the same spreadsheet — `Races` (created automatically by `apps-script/Code.gs` on first use), independent of the session-log sheet. Same fire-and-forget write / `cors` read pattern as sessions: writes are `POST` bodies shaped `{kind:'race', action:'add'|'delete', race:{...}}`; reads are `GET .../exec?races=1`. The race `id` is generated client-side (`index.html`) specifically so a delete never has to read an id back out of an opaque no-cors response.
- **The training plan uses a third sheet tab** — `Plan` (also created on demand, by `getPlanSheet_()`), one row per planned session. Same pattern again: writes are `{kind:'plan', action:'add'|'addBatch'|'delete'|'deleteWeek', ...}`, reads are `GET .../exec?plan=1`, ids are client-generated by `newPlanId()`. The two extra actions exist because a week is ~10 sessions and firing 10 concurrent no-cors POSTs races on `appendRow` — see decision 28.
- **Strava is already syncing into the session sheet, and nothing reads it back.** `apps-script/Code.gs` contains a complete Strava OAuth flow (`handleStravaCallback_`) plus a 2-hourly `syncStravaActivities` trigger that writes objective columns — `ActualMin, ActualKm, AvgHR, AvgPaceOrPower, ElevM, SufferScore` — into columns N–T of the session sheet. But `listSessions_` only reads columns 1–13, so none of it ever reaches the app. This is a disposable Apps Script precursor to the target architecture's Session 7 Garmin/Strava work, not that work itself. Worth knowing it exists: `ActualMin` is a genuinely objective duration, which is the obvious future input to planned-vs-actual comparison (currently self-reported).

### Splash
First thing shown on every load — full-bleed `--blue` background, the `favicon.png` logo (no drop shadow), and a headline ("Helping you prepare for your next race") styled to the page-title spec, directly under the logo. No separate DM Mono sub head any more — the earlier "Helping triathletes progress" sub head plus "Mobility, intensity, fuelling, strength" headline were both retired in favour of this single race-focused line (see decision 22). The base nav is visible underneath it, same as it is under the training-phase screen. No timer, no auto-advance — it's dismissed only by tapping a nav tab. Tapping **Check-in** from the splash routes to the training-phase picker first if no phase is set yet (see below); tapping any other tab bypasses straight to that view, same as tapping a tab from the phase screen always has. Below the headline, a **race countdown card** shows the nearest upcoming entry from the Race calendar (name + "N weeks · N days to go") — hidden entirely, not shown empty, when no race is saved (see decision 22). Directly beneath it sits a second card in the identical translucent treatment, the **"Today" card** (`renderSplashToday()`, `.splash-today`), reading today's sessions off the training plan: one tappable row per planned session **still to log** (`TODAY · {Disc} — {title} · {N} min · {intensity}`), or "TODAY · Rest day" when the current Mon–Sun week has a plan but today isn't in it, or — when nothing at all is planned for the week — **no node in the DOM at all**, which is a stricter hide than the countdown card's `[hidden]` toggle. Tapping a row starts a fresh check-in pre-filled from that session (see decision 29). A planned session that has **already been logged** is still listed, after the open ones, but as a static row marked "Logged" rather than a tappable one — see decision 34. Both cards are `width:100%` capped at 300px so they align rather than stacking at two different content-driven widths.

### Navigation — 5 tabs, plus 2 link-reached views
The bottom nav is exactly five buttons, in this left-to-right order (`tab-warmup`, `tab-strength`, `tab-flow`, `tab-dash`, `tab-races` — see decision 22 for the ordering rationale):

| Tab | What it does |
|---|---|
| **Warm up** | Discipline-specific mobility routines (Swim / Bike / Run sub-tabs) with a checklist and auto-detected completion — see decision 17. |
| **Strength** | A core tab, not a content tab — renders two purpose-built strength PWAs via `<iframe>`: `TRI-swim-strength mobile app/` and `TRI-run-strength mobile app/` (each its own `index.html`/`app.js`/`manifest.webmanifest`/service worker). They're separate deployable units for build/versioning reasons, not a bolt-on — they ship in this repo, deploy under the same GitHub Pages site, and are part of the product's core navigation. Sub-tabs: Swim / Ride-Run. |
| **Check-in** | The primary interaction — a stepped, after-session logging wizard ("Just finished", step counter). Not a pre-scheduled calendar; purely reactive logging. **When today has a planned session, opening Check-in lands on a session-picker screen first** (`state.mode === 'plan'`), with "Log an unplanned session" as the explicit escape hatch — see decision 32. With nothing planned today, it opens straight into the wizard as before. |
| **This week** | Weekly dashboard: discipline breakdown grid, phase pills, signal sections (see below), week number. Under the **Adherence** subtab only, a "This week's plan" section lists all 7 days with a per-session Edit / per-empty-day Add linking into the Plan view — see decision 31. |
| **Races** | Race calendar — add/remove upcoming (and past) races with name, date, optional website and notes. Synced via the Sheet's `Races` tab (see Storage & sync). Drives the splash's countdown card. Far-right tab (see decision 22). |

**Month and Plan are *not* tabs.** Both are full views (`showView('trends')` / `showView('plan')`) reached only by a link inside This week — "See this month →" (`goToMonth()`) and "Edit plan →" (`goToPlan()`), both styled `dash-signal-link`. The nav is a fixed five buttons at 84px; a monthly roll-up and a plan editor visited about once a week don't earn permanent nav space. This was already true of Month before the plan feature existed — the table above used to list Month as a tab, which was never accurate, and omitted Warm up, which always has been one.

| Link-reached view | What it does |
|---|---|
| **Month** (`view-trends`) | Same signal/dashboard machinery as "This week," rolled up by week blocks across the month. |
| **Plan** (`view-plan`) | Manual entry of the athlete's own planned sessions, a week at a time, with copy-week-forward and clear-week. Each session card carries **Edit** (opens a pre-filled form; every field except the date) and Remove. Synced via the Sheet's `Plan` tab. See decisions 28 and 31. |

### Training phase
Asked **once, ever** (not resolved from dates) via a simple picker: `Base / Taper`, `Build 1`, `Build 2`, `Peak`. Changeable any time from the dashboard, **or from the check-in flow itself** — the flow header shows the current phase with a "Change phase" link on every question (hidden only on the post-save "logged" screen, where the eyebrow already shows it). Picking a new phase there returns straight back to the question you were on. There is no `training_start_date`, no `race_date`, no `block_config`, and no per-day content resolution — the target's `resolvePosition()`/`phasePosition` system does not exist in this app.

Every other tab (This week, Month, Strength, Warm up) can be reached without ever setting a phase — the bottom nav bypasses the phase screen for those. **Check-in is the exception**: since a logged session is stamped with whatever phase is current, tapping Check-in with no phase set routes to the phase picker first rather than bypassing it, then returns straight to Check-in once a phase is chosen. Returning users who already have a phase saved skip straight to Check-in as before — the in-flow "Change phase" link exists precisely so they aren't stuck with whatever they picked when they first opened the app.

### Check-in data model
Each logged session (`log` array in `localStorage`) captures: `date`, `disc` (`swim`|`bike`|`run`|`brick`|`strength`), `duration`, `intensity`, `rpe`, five single-answer questions (`a1`, `a2`, `a3`, `b2`, `b3`), a free-text `note`, and the currently-set `phase`. There is no `tips`/`prompt`/`mechanism`/contrast-pair content shown alongside a session — nothing from `tips.js` is used.

The five answer fields are **not** "three intent questions plus two fuelling ones" — an earlier version of this doc said that and it was wrong in both directions. What they actually are, confirmed against the Sheet headers (`VsIntent, VsPlan, Fuelling, Feeling, Niggle`) and against which function reads which field:

| Field | Question | Read by |
|---|---|---|
| `a1` | Did it finish at the effort you intended? | `driftCat()` → the **Intensity drift** signal |
| `a2` | Did you do it as planned? (`planned`/`short`/`sub`/`didnt`) | `planCat()` → the **Adherence** signal |
| `a3` | Fuelling — asked on long sessions only | `fuelCat()` → the **Fuelling** signal |
| `b2` | How did it feel (affect)? | not read by any signal |
| `b3` | Any pain, tightness or niggle? | not read by any signal |

Fuelling is `a3`, not `b2`/`b3`. This matters: `a2` is the field the self-reported Adherence signal is computed from.

### Signals (replaces "weekly reflection")
Instead of a self-reported readiness score + grey-zone %, the app derives **rule-based signals** from logged sessions:
- **Fuelling** and **Intensity drift** categories, each with a severity (`good`/`mid`/`bad`)
- **Compound-pair detection** — e.g. two consecutive over-intent sessions — flagged as a distinct, higher-priority pattern than an isolated one
- Auto-generated plain-language headlines per signal (e.g. "X times this week, a session that ran well over intent was followed by another one running well over intent…")

This is a different mechanism from the target's `readiness`/`grey_zone_pct`/`vuln_session` self-report fields — it's inferred from logged data, not asked directly.

### Design system
Shares the same CSS custom properties as documented in [style-guide.md](style-guide.md) (discipline colours, DM Sans/DM Mono, card/radius conventions) — **that file is accurate for both current and target.**

### PWA
Manifest is inlined as a `data:` URI in the `<head>` (not a separate `public/manifest.json` + `sw.js` pair as the target architecture specifies).

---

## Target tech stack (not yet implemented)

| Layer | Choice | Reason |
|---|---|---|
| Frontend | React + Vite | Fast dev server, clean component model, easy PWA setup |
| Styling | CSS custom properties (no Tailwind) | Design system already established — preserve it exactly |
| Backend | Hono on Node.js | Lightweight, edge-compatible, minimal boilerplate |
| Database | Supabase (Postgres) | Auth + database + row-level security in one. Free tier sufficient for launch. |
| Auth | Supabase Auth | Magic link + Google OAuth. No passwords to manage. |
| Payments | Stripe | Checkout + webhooks. Subscription status written to Supabase. |
| Hosting | Vercel (frontend) + Fly.io (backend) | Both have generous free tiers. Vercel handles PWA edge cases well. |
| Garmin | Unofficial garminconnect Python wrapper OR official OAuth API (future) | Backend-only — never expose credentials client-side |

`frontend/` is scaffolded (components exist: `Today.jsx`, `Week.jsx`, `Tips.jsx`, `Calendar.jsx`, `Onboarding.jsx`, `Auth.jsx`, `Upgrade.jsx`) but not wired: no `supabase-js` dependency, no `frontend/src/lib/supabase.js`, no `frontend/src/hooks/`, `Auth.jsx`'s magic-link form doesn't call any API, and `App.jsx`'s screen state machine never actually routes to it. `backend/` contains only `.env.example`, `.gitignore`, and an unrelated standalone script — no Hono server exists yet.

---

## Repository structure

```
tri-momentum/
├── CLAUDE.md                  ← this file
├── style-guide.md              ← design system source of truth — colour tokens, type, layout, component patterns
├── TRI-Momentum-Design-Guide.html  ← browsable visual rendering of style-guide.md
├── index.html                 ← ★ CURRENT LIVE APP (shell) — static, self-contained, deployed as-is via GH Pages
├── apps-script/
│   └── Code.gs                ← Google Apps Script Web App — receives session POSTs, writes to the athlete's Sheet
├── TRI-swim-strength mobile app/   ← ★ CORE — Strength tab's Swim sub-tab, rendered via iframe in index.html
│   ├── index.html / app.js / styles.css / manifest.webmanifest / sw.js
├── TRI-run-strength mobile app/    ← ★ CORE — Strength tab's Ride/Run sub-tab, same pattern
│
│  ─── target architecture (scaffolded, not yet wired up) ───
│
├── .env.example               ← all required env vars documented here
├── .env.local                 ← never committed
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Today.jsx
│   │   │   ├── Week.jsx
│   │   │   ├── Tips.jsx
│   │   │   ├── Calendar.jsx
│   │   │   ├── Onboarding.jsx
│   │   │   ├── Auth.jsx           ← sign in / sign up / magic link (currently disconnected)
│   │   │   ├── Upgrade.jsx        ← paywall prompt component (currently no-op)
│   │   │   └── shared/
│   │   │       ├── BottomNav.jsx
│   │   │       ├── SessionCard.jsx
│   │   │       └── PhasePill.jsx
│   │   ├── data/
│   │   │   ├── tips.js            ← block-relative tip content (static, see schema below) — unused by current app
│   │   │   ├── raceProximity.js   ← race-proximity overlay content (23 cards, 5 zones) — unused by current app
│   │   │   ├── phases.js          ← PHASES_39 and PILLAR_CYCLE constants
│   │   │   └── insights.js        ← CADENCE_INSIGHTS constant
│   │   ├── hooks/                 ← does not exist yet (useAuth.js, useProfile.js, useReflections.js planned)
│   │   ├── lib/
│   │   │   └── tipResolver.js     ← resolvePosition(), resolveTip(), resolveProximityZone(), canAccessPhase() — written but not called anywhere
│   │   ├── App.jsx                ← localStorage only; auth screen unreachable
│   │   └── main.jsx
│   └── vite.config.js
├── backend/                    ← empty scaffold: .env.example + .gitignore only, no server code
└── supabase/                   ← does not exist yet (no migrations applied, no project schema)
```

---

## Design system — do not deviate

Colour tokens, typography, layout rules, and component patterns now live in **[style-guide.md](style-guide.md)**, not here — that's the single source of truth for the visual language, applies to both the current app and any target-architecture work, and is what [TRI-Momentum-Design-Guide.html](TRI-Momentum-Design-Guide.html) is compiled from. Never introduce Tailwind, Bootstrap, or component libraries that override those tokens. This file (CLAUDE.md) keeps the product/architecture decisions and their "why" — see Key product decisions below for the design-relevant ones (16, 18, 20, 21).

---

## 🔮 Freemium model — target, future phase, not implemented today

The current app is entirely free, local-only, and has no accounts. Everything below describes the intended monetisation model once the target architecture (Supabase + Stripe) is built — treat it as the plan, not a spec for the live app.

> **Note:** the data-persistence side of this plan now assumes the adopted `session_checkins` model (see Database schema), so "reflection"/"readiness"/"grey zone" below refer to that model's equivalents (check-in history, computed signals), not the original self-report fields. The **content side** — gating Build/Peak/Recovery tip cards — still assumes the `tips.js` phase-content system gets built, which is a separate, still-unresolved question (see Content system). If that system is never built, this whole gate needs rethinking — Option B (below) becomes the more natural fit, now as a check-in/signal-history gate rather than a reflection gate.

### Free tier
- Onboarding (mode selection, training start date, race date, calendar setup)
- Tips: **Base phase only** — all 4 block weeks, all 6 pillars (full first training block)
- Race-proximity overlay: **not included** — taper, race week, and post-race cards are paid
- Check-in logging (the `session_checkins` flow) — **in-session only, no persistence**
- Today tab: phase pill, cadence selector, session cards
- Calendar tab: full access (they need this to use the app at all)

### Paid tier — "TRI Momentum Pro" ($9.99 AUD/month or $79 AUD/year)
- All phases: Build, Peak, Recovery (in addition to Base)
- Race-proximity overlay — all 5 zones (awareness, taper, race week, race day, post-race)
- Full check-in history persistence (`session_checkins` rows saved and synced across devices, not just held in-session)
- Cross-device sync
- Trend analytics (fuelling / intensity-drift signal trends over time, compound-pair pattern history)
- Garmin integration (when built)
- Future: club features

### Why the Base phase gate
The Base phase covers 4 block weeks — a complete training block with all 6 pillars and a recovery week. The paywall hits at the natural phase boundary when training intensifies (Build phase begins), which is the highest-intent upgrade moment. The conceptual split is clean: "free = learn the foundations of training structure, paid = train for a race."

**Alternative model (Option B — reflection gate):** gate on reflection saving and trend analytics rather than content. All tip content free; gate on what the app does with the athlete's history over time. Converts on data-loss fear rather than content scarcity — stronger long-term LTV argument. Evaluate at first 100 paid users.

### Paywall behaviour
- **Never** show blurred/locked content. Free users see only Base phase cards — the Tips tab ends cleanly with an upgrade prompt at phase boundary.
- Phase gate is enforced via `canAccessPhase(phase, subscriptionTier)` in `tipResolver.js` — **this function is already written but not called from anywhere yet.**
- **Upgrade prompt triggers:**
  1. Attempting to view a Build, Peak, or Recovery tip card
  2. Attempting to persist a check-in past the current session (Check-in flow → save)
  3. Attempting to view the trend chart (Tips tab)
- Upgrade prompt is a bottom sheet component (`<Upgrade />`) — not a page redirect. **Currently presentational only; its buttons have no handlers.**
- After successful Stripe checkout, Supabase `subscription_tier` updates via webhook and the UI responds immediately on next auth check

### Checking subscription tier
```js
// Always derive from Supabase session — never trust client-side state alone
const { data: profile } = await supabase
  .from('user_profiles')
  .select('subscription_tier, subscription_expires_at')
  .eq('user_id', user.id)
  .single()

const isPro = profile.subscription_tier === 'paid'
  && new Date(profile.subscription_expires_at) > new Date()
```

---

## 🔮 Database schema — target, not yet applied

No `supabase/migrations/` exist yet and no Supabase project schema has been created. This is the plan for when Phase 2/3 of the multi-user build begins. All tables use Supabase row-level security (RLS). Users can only read and write their own rows.

```sql
-- 001_initial_schema.sql

create table public.user_profiles (
  id uuid primary key references auth.users(id) on delete cascade,
  email text,
  subscription_tier text not null default 'free', -- 'free' | 'paid'
  subscription_expires_at timestamptz,
  stripe_customer_id text,
  mode text, -- 'race' | 'general'
  race_date date,
  training_start_date date, -- used by resolvePosition() to determine current phase/block week
  block_config jsonb, -- { base: N, build: N, peak: N, recovery: 1 } — set during onboarding
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Superseded the original session_logs + week_reflections tables below — the
-- current index.html app's check-in model won out (see "Key product
-- decisions"). Mirrors the shape of the localStorage `log` array in
-- index.html directly, so the eventual Supabase migration is a straight
-- copy of existing data, not a remodel.
create table public.session_checkins (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  session_date date not null,
  disc text not null, -- 'swim'|'bike'|'run'|'brick'|'strength'
  duration integer, -- minutes
  intensity text,
  rpe integer,
  a1 text, -- execution/intent question — drives the intensity-drift signal
  a2 text,
  a3 text,
  b2 text, -- fuelling question — drives the fuelling signal
  b3 text,
  note text,
  phase text, -- 'base'|'build1'|'build2'|'peak' — whatever was current when logged, not resolved from dates
  logged_at timestamptz not null default now()
);

-- Signals (fuelling, intensity drift, compound-pair detection, severity,
-- auto-generated headlines) are computed at read time from
-- session_checkins, exactly as index.html does today — not stored.
-- Don't add a signals/reflections table unless a caching or
-- historical-snapshot need is actually proven out first.

-- Future table — add in a later migration
-- create table public.garmin_activities ( ... );

-- RLS policies (repeat pattern for each table)
alter table public.user_profiles enable row level security;
create policy "Users can read own profile"
  on public.user_profiles for select using (auth.uid() = id);
create policy "Users can update own profile"
  on public.user_profiles for update using (auth.uid() = id);

-- (repeat for session_checkins)
```

**Resolved:** the target schema adopts the current app's check-in + computed-signals model (`session_checkins`), not the original `session_logs`/`week_reflections` self-report design. `calendar_sessions` and `week_cadences` are dropped entirely — they described proactive day-of-week planning that neither the current app nor the adopted model implements. If calendar-based planning is ever wanted, design it fresh against actual need rather than reviving these.

---

## 🔮 Authentication flow — target, not implemented today

The current app has no accounts (see Current implementation). This is the plan for the target architecture.

- **Sign up:** magic link (email only — no password friction at sign-up)
- **Sign in:** magic link or Google OAuth
- **Session:** Supabase handles JWT refresh automatically
- **On first sign-in:** create `user_profiles` row via Supabase trigger or `useEffect` in `useAuth.js`
- **Auth state:** stored in React context via `useAuth` hook, available app-wide
- **Unauthenticated users:** can use the app in free/local mode (no persistence). Upgrade prompt appears when they hit a save action.

```js
// useAuth.js pattern
const { data: { session } } = await supabase.auth.getSession()
const user = session?.user ?? null
const isAuthenticated = !!user
```

---

## 🔮 Stripe integration — target, not implemented today

### Products to create in Stripe dashboard
- **TRI Momentum Pro Monthly** — $9.99 AUD / month, recurring
- **TRI Momentum Pro Annual** — $79.00 AUD / year, recurring

### Checkout flow
1. User taps upgrade prompt → `<Upgrade />` bottom sheet opens
2. User selects monthly or annual
3. Frontend calls `POST /api/stripe/create-checkout-session` with `{ priceId, userId }`
4. Backend creates Stripe Checkout session with `success_url` and `cancel_url`
5. Frontend redirects to Stripe Checkout
6. On success, Stripe fires `checkout.session.completed` webhook
7. Backend webhook handler updates `user_profiles.subscription_tier = 'paid'` and sets `subscription_expires_at`
8. User redirected back to app — `useProfile` hook re-fetches, UI unlocks immediately

### Webhook handler (backend/src/routes/stripe.js)
```js
// Always verify webhook signature — never skip this
const sig = req.headers['stripe-signature']
const event = stripe.webhooks.constructEvent(body, sig, process.env.STRIPE_WEBHOOK_SECRET)

if (event.type === 'checkout.session.completed') {
  // update supabase user_profiles via admin client
}
if (event.type === 'customer.subscription.deleted') {
  // downgrade to free
}
```

---

## PWA requirements

- **Current app:** manifest is inlined as a `data:` URI in `index.html`'s `<head>`; no separate service worker for the main app (the two Strength sub-apps each have their own `manifest.webmanifest` + `sw.js`).
- **Target (not yet built):** `manifest.json`: name "TRI Momentum", short_name "TRI", theme colour `#0022FF`, background `#F7F8FC`, display `standalone`, orientation `portrait`. Service worker: cache-first for static assets, network-first for API calls. Offline: Today and Week tabs render from cached data when offline; Tips tab static content always cached; saves queue when offline and sync on reconnect. Install prompt triggers after second session, not on first visit.

---

## 🔮 Content system (`tips.js` / `raceProximity.js`) — target, unused by current app

**None of this is used by the shipped app today.** The current app has no phase-aware prompt content at all — it's a check-in + signal-detection tool (see Current implementation). This section describes the content system as planned for the target `frontend/` architecture, written but not wired up (`tipResolver.js` exists; nothing calls it).

The tip system is intended to be the product's primary intellectual property, once built. Content would live in two static JS files bundled with the app, never fetched from a database.

### Content files (target)

| File | Purpose |
|---|---|
| `frontend/src/data/tips.js` | Block-relative tip content — `CARD_CONTENT[phase][phasePosition][pillar][day]` |
| `frontend/src/data/raceProximity.js` | Race-proximity overlay — `RACE_PROXIMITY_CONTENT[zone][key]` |
| `frontend/src/lib/tipResolver.js` | Runtime resolver — `resolvePosition()`, `resolveTip()`, `resolveProximityZone()` etc. |

### Updating tip content

**Do not embed tip copy in CLAUDE.md.** If/when this system is activated, all content lives exclusively in the two data files above:

1. Claude Code reads the relevant data file at the start of the content session
2. Edits are made directly to that file
3. CLAUDE.md is never touched for content changes — only for architectural decisions

### tips.js schema (v3 — phasePosition)

```js
// frontend/src/data/tips.js
export const CARD_CONTENT = {

  // Level 1: phase — 'base' | 'build' | 'peak' | 'recovery'
  base: {

    // Level 2: phasePosition — 'early' | 'mid' | 'late' | 'only'
    //   early  first week of a 2+ week phase
    //   mid    middle weeks of a 3+ week phase (repeats for longer phases)
    //   late   final week of any 2+ week phase
    //   only   phase is exactly 1 week (recovery always uses 'only')
    early: {

      // Level 3: pillar
      foundations: {
        tue: { title, mech, prompt, good, med },
        thu: { title, mech, prompt, good, med },
        sun: { title, mech, prompt, good, med },
      },
      technique:   { tue: {...}, thu: {...}, sun: {...} },
      // ... all 6 pillars
    },
    mid:  { /* same 6 pillars */ },
    late: { /* same 6 pillars */ },
  },
  build:    { early: {...}, mid: {...}, late: {...} },
  peak:     { early: {...}, mid: {...}, late: {...} },
  recovery: { only: {...} },  // single position only
};
```

**Block structure:** variable. Athlete sets `block_config` during onboarding:
```js
// stored in user_profiles.block_config
{ base: N, build: N, peak: N, recovery: 1 }
// ranges: base 1–8, build 1–4, peak 1–4, recovery always 1
```
Total block length = sum of all four. Repeats indefinitely.

**Phase position resolution** (from `resolvePhasePosition(weekInPhase, phaseLength)` in tipResolver.js):

| Phase weeks | Wk 1 | Wk 2 | Wk 3 | Wk 4+ (last) |
|---|---|---|---|---|
| 1 | only | — | — | — |
| 2 | early | late | — | — |
| 3 | early | mid | late | — |
| 4+ | early | mid | mid… | late |

**Pillar emphasis matrix** (which pillar leads per phase × phasePosition):

| Phase | early | mid | late | only |
|---|---|---|---|---|
| base | foundations | technique | durability | foundations |
| build | intensity | technique | durability | intensity |
| peak | racecraft | intensity | durability | racecraft |
| recovery | — | — | — | durability |

All 6 pillars are populated for every phase × position slot. The emphasis pillar is surfaced on the Today tab; others are available via the Tips tab.

### raceProximity.js schema

```js
// frontend/src/data/raceProximity.js
export const RACE_PROXIMITY_CONTENT = {
  awareness: { tue, thu, sun },           // 15–21 days out — supplements block tip
  taper:     { tue, thu, sun },           // 8–14 days out  — equal weight with block tip
  raceweek:  { 7, 6, 5, 4, 3, 2 },       // keyed by days_to_race — replaces block tip
  raceday:   { morning },                 // race morning — replaces block tip
  post:      { 1, 2, 3, 7 },             // keyed by days_since_race — replaces block tip
};
```

### Tips.jsx render decision pattern

```js
// Resolve position — requires both training start date and block config
const pos  = resolvePosition(user.trainingStartDate, user.blockConfig);
const zone = resolveProximityZone(user.nextRaceDate);

if (zone === 'raceweek' || zone === 'raceday' || zone === 'post') {
  // Full replacement — block tip not shown
  const key = zone === 'raceweek' ? daysToRace(user.nextRaceDate)
            : zone === 'post'     ? daysSinceRace
            : 'morning';
  tip = resolveProximityTip(RACE_PROXIMITY_CONTENT, zone, key);
} else if (zone === 'taper' || zone === 'awareness') {
  // Supplement — both shown, proximity card first
  proximityTip = resolveProximityTip(RACE_PROXIMITY_CONTENT, zone, dayOfWeek);
  blockTip     = resolveTip(CARD_CONTENT, pos.phase, pos.phasePosition, pos.emphasisPillar, dayOfWeek);
} else {
  // Normal block tip only
  tip = resolveTip(CARD_CONTENT, pos.phase, pos.phasePosition, pos.emphasisPillar, dayOfWeek);
}

// Gate check
if (!canAccessPhase(pos.phase, user.subscriptionTier)) {
  // show <Upgrade /> prompt
}
```

### Authoring principles for tip content

- **Mechanism first.** Every tip explains *why* before *what*. Athletes who understand the mechanism self-correct — athletes who only get the instruction forget it under fatigue.
- **No hand-holding.** The target user has strong self-knowledge and finds generic coaching advice patronising. Write for someone who will push back if the reasoning is weak.
- **Specificity over generality.** "Drop 10 BPM and observe how Monday feels" beats "train in Zone 2". Name the number, the session, the observation.
- **The contrast pair is not good vs bad.** It is sharp vs mediocre. The mediocre response is defensible — it's what a reasonable athlete does when they haven't thought carefully. That's what makes it useful.
- **Sunday prompts drive the weekly reflection.** *(Stale pending reconciliation — the "readiness"/"grey-zone" inputs this originally fed no longer exist; the adopted model computes signals from `session_checkins` instead. If this content system is ever built, decide what the Sunday card should feed into under the adopted model before writing to this principle again.)*
- **Content is phase-aware, not week-number-aware.** "Base Wk1 Foundations" is always about establishing the floor under fresh legs — regardless of what month of the year it is. Never write content that assumes a specific calendar date.

---

## Session build order (target architecture)

This is the plan for building out `frontend/`/`backend/` toward the target. It does **not** describe work on the current `index.html` app, which is developed directly and iteratively without this sequence.

| Session | Scope | Done? |
|---|---|---|
| 1 | Scaffold Vite + React, establish file structure | ☐ (scaffold exists) — data model resolved (adopt current app's check-in/signals model, see Database schema); **UI/tab reconciliation still open**: whether `frontend/` rebuilds the current Strength/Check-in/This week/Month tabs, the original Today/Week/Tips/Calendar tabs, or some merge |
| 2 | Supabase integration — auth (magic link + Google), user_profiles table, useAuth hook | ☐ |
| 3 | Migrate all state from localStorage to Supabase — `session_checkins` | ☐ |
| 4 | Freemium gating — phase content wall (if `tips.js` is built) or check-in/signal-history wall (Option B), Upgrade bottom sheet | ☐ |
| 5 | Stripe integration — checkout, webhooks, subscription status, monthly + annual pricing | ☐ |
| 6 | PWA — manifest, service worker, offline support, install prompt | ☐ |
| 7 | Garmin integration — backend OAuth or unofficial wrapper, activity import, session auto-population | ☐ |
| 8 | Club features — shared leaderboard, club admin, member management (scope TBD) | ☐ |

---

## What not to build

- No native iOS/Android app — PWA is the target. Do not introduce React Native.
- No Tailwind — the design system uses CSS custom properties. Tailwind would fight it.
- No server-side rendering — the target is a client-side React SPA with a thin Hono API backend; the current app is static HTML with no server at all.
- No admin dashboard for now — manage via Supabase dashboard directly (once it exists).
- **No in-app training plan *generation*.** The app never authors, suggests, or adjusts a session — the athlete's own periodisation cycle is authored in Notion and stays there. What the app *does* hold, since decision 28, is a **manually-entered copy** of their planned sessions, typed in the same way races are, so it can surface what's on today and compute adherence against a real reference point. Deliberately excluded: auto-generating a plan from any source (Notion API, file import, AI generation), bidirectional Notion sync, a drag-and-drop calendar editor, **changing a planned session's date** (that *is* the drag-and-drop calendar editor, reached through a different door — see decision 31), and multi-cycle plan libraries. Editing a planned session's other fields in place *is* now supported, as of decision 31, which narrows the original blanket "no editing past plans" exclusion to date only. The distinction that matters: **the app stores what the athlete planned; it never decides what they should plan.** This is not the `tips.js` phase-content system, which remains unbuilt and unused.

---

## Environment variables (target — the current app needs none of this)

The current `index.html` app has no env vars; its only external dependency is the hardcoded Google Apps Script Web App URL for Sheet sync. Everything below is for the target architecture, once built.

```bash
# frontend/.env.local
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_STRIPE_PRICE_MONTHLY=
VITE_STRIPE_PRICE_ANNUAL=

# backend/.env.local
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=   # admin key — never expose to frontend
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PORT=3001
```
23. **Typography moved from DM Sans + DM Mono to a single family, Outfit.** `--fd`, `--fb`, and `--fm` in `index.html` all point to `'Outfit',sans-serif` now — the three token names stay (they still communicate "what role is this text playing": display, body, mono-style label), only the value changed. With one typeface, the earlier "DM Sans for what a human reads, DM Mono for what the interface says about itself" split is now carried entirely by weight/case/letter-spacing instead of a family switch — eyebrows, dates, and other label-style text stay uppercase with wide tracking (still informally called "mono" by class name, e.g. `.race-month-label`, even though it's no longer a literal monospace font). Google Fonts `<link>` swapped from `DM+Sans:ital,wght@0,400;0,500;0,600;1,400&family=DM+Mono:wght@400;500` to `Outfit:wght@400;500;600` (Outfit has no italic on Google Fonts — `font-style:italic` on the one place that uses it, `.review-a.none`, now renders as a browser-synthesized oblique, which is visually fine). `TRI-Momentum-Design-Guide.html` embeds its own fonts as base64 (a fully self-contained static file, no network dependency) — its four DM Sans/DM Mono `@font-face` blocks were replaced with one Outfit variable-font `@font-face` (weight range 100–900, latin subset only, matching the file's existing single-subset convention), and every `font-family:'DM Sans'`/`'DM Mono'` declaration in its component previews was repointed to `'Outfit'`. Its Type section intro was rewritten, since "two families doing two different jobs" no longer describes the system — also fixed a stale pre-existing inaccuracy found in passing: the design guide's own Layout section still said "64px fixed height, 4-column" for the bottom nav, which `style-guide.md` had already been corrected to 84px/5-tab earlier the same day (decision 22) but the HTML guide was never brought back in sync until now. **Add a race toggle:** dropped the "+" from the button label — just "Add a race" — since the plain label reads clearly enough on its own; the reference app's own button keeps its "+", this app's doesn't. **Race website, in full:** race cards now show the complete stored URL exactly as entered (`race.url` directly, e.g. "https://swimrun.com.au/events/sydney-north/") rather than a parsed host+path ("swimrun.com.au/events/sydney-north/") — `raceWebsiteLabel()` was removed from `index.html` since nothing computes a shortened form any more.
24. **`--ink3` darkened app-wide — the original failed WCAG AA contrast.** A review of the race card's grey text (prompted by the spacing pass below) turned up that `--ink3` (`#8B93B8`, "tertiary/placeholder" grey) only hit 3.02:1 against white and 2.84:1 against `--bg` — both well under the 4.5:1 AA minimum for normal text. This is the app's *primary* label colour — `.eyebrow`, `.dash-eyebrow` (every page's own heading style), dates, hints, placeholders, disabled-button text, all 20 usages across `index.html` — so it wasn't a Races-only problem. Fixed at the token level rather than with a race-card-specific override: `--ink3` → `#626B8C` (5.25:1 on white, 4.95:1 on `--bg`, 4.55:1 on `--blue-l`), so every screen using "tertiary grey" got fixed at once instead of Races ending up a different, one-off shade of grey from everywhere else. `--ink2` (7.56:1) and `--ink` (18.86:1) were already compliant and untouched. Mirrored into `TRI-Momentum-Design-Guide.html`'s own `--ink3`/`--page-ink3` tokens and its "Ink 3" swatch hex label, since that file hardcodes its own copy of the palette rather than reading `index.html`'s CSS. New "Accessibility" section added to `style-guide.md` documenting the 4.5:1/3:1 contrast rule as a standing check for any new colour. **Race card spacing:** the first pass's internal margins (2–10px between date/name/pill/notes/website/remove) read as cramped once real content was in front of it — padding went 14px×15px → 18px×16px, and every internal gap roughly doubled (name row 5px→10px, status pill 8px→12px, body 2px→12px, notes 6px→10px, section label 10px→16px, website link 3px→6px, foot 10px→16px).
25. **Race card restructured into a two-band header — banner headline + grey meta band — matching the reference club-calendar app's race card more closely.** Previously the card's `<summary>` was a flat stack (date/countdown line, then name+chevron, then pill) all on the card's own white background. Now: a **banner** (`--blue-l` fill, same tint on every card — the nearest-upcoming distinction moved to the card's outer ring/shadow, not the banner colour) holds the race name as a bold `--ink` Outfit 17/600 headline with the **commitment pill inline right next to it** (wraps onto its own line via `flex-wrap` on long names) and an `--ink` chevron; directly below it, a **grey meta band** (`--s3` fill) holds the date and countdown on one row. Both bands sit inside `<summary>`, so both stay visible even collapsed — nothing about "what race, what commitment, how far out" needs an extra tap. `.race-card` gained `overflow:hidden` so the two full-bleed bands clip to the card's rounded corners; padding moved off the card itself and onto each band/the body individually. Went through an intermediate white-text-on-dark-banner version first (`--ink` fill for regular cards, `--blue` for is-next, white headline/chevron) before landing here — banner colour flattened to a single `--blue-l` tint with dark text for every card, on request, since a light banner reads calmer against the page and the ring already carries the "is-next" signal. **"Considering" pill recoloured** from `--modified`/`--modified-l` (amber) to `--phase-build`/`--phase-build-l` (tangerine), on request — this surfaced a pre-existing contrast gap, fixed in decision 26 below.
26. **`--phase-build` darkened app-wide — the original failed WCAG AA contrast, surfaced by decision 25's pill recolour.** `--phase-build`/`--phase-build-l` only reached 3.23:1 (text this size — 11.5px bold — needs the 4.5:1 bar, not the 3:1 large-text one), under the AA minimum decision 24 established. It wasn't a new gap: the Month week phase pill had been using this exact pairing for Build all along, just unnoticed until the "Considering" race pill put the same combination in front of the same check a second time. Even white text on the original `#D9660B` only reached 3.59:1, so a text-colour swap alone wouldn't have been enough — darkened the background itself, token-level, to `#A64C09` (5.17:1 on `--phase-build-l`, 5.74:1 on white), fixing both usages (Month week Build pill and the "Considering" race pill) together. Still reads as the same tangerine, just a shade darker. Checked `--phase-base` (5.46:1 on `--phase-base-l`) and `--phase-peak` (6.03:1 on `--phase-peak-l`) while in there — both already comfortably compliant, so `--phase-build` was the only phase colour with the gap.
27. **This week/Month "by session" grid cells now render every session logged that day for a discipline, not just the day's last one.** Previously a grid cell's colour came from `dayEntry.session` — the *last* entry logged for that calendar day, filtered to whichever discipline row it happened to match — so a second same-discipline session on the same day (e.g. two bike sessions Thursday) silently disappeared from the grid entirely, and the day's `entries` array (already used elsewhere for counts) went unused here. `dashCellClass()` was replaced with `dashCell()`, which filters `dayEntry.entries` to the row's discipline and renders one `.dash-cell-seg` bar per matching entry inside the `.dash-cell` container (now a flex row with a 2px gap) instead of colouring the cell itself — a day with one matching session still renders as a single full-width bar (visually identical to before), a day with two or more renders as narrower side-by-side bars, each independently coloured `good`/`mid`/`bad`/`na` off its own session's `catFn` result. No markup/JS changes needed for `intent`, `intensity-drift`, or `fuelling` scoring — `catFn` already took a single session and needed no changes to run per-entry instead of once per day.
28. **The app now holds a manually-entered copy of the athlete's training plan — reversing the old "no in-app training plan" line in What not to build.** The reversal is narrower than it sounds: the app still never *generates* a plan. Notion stays the authoring surface; this is transcription, so the app has an independent reference point to measure against instead of scoring every session against an intent self-reported in the same breath as the outcome. **Storage is dated rows, not a cycle-relative template** — each planned session carries a real ISO `date`, so joining plan to log is the same `filter(e => e.date === dISO)` that `buildWeek()` already does, with no resolver, no cycle metadata to store, and no revival of the unbuilt target architecture's `resolvePosition()`/`phasePosition` machinery, which this app has consistently rejected. The accepted trade-off is that repeating a block writes N weeks of rows rather than one template — mitigated by copy-week, and trivial in volume (a 4-week cycle at ~10 sessions/week is 40 rows). It also makes the "no editing past plans" rule self-enforcing: changing a template can't retro-edit rows that already exist. *(Superseded in part by decision 31, which added a real per-row `update` action. The template-vs-rows reasoning still holds — there is still no template to retro-apply — but a single planned session's fields can now be edited in place. Its date still cannot.)* **A third sheet tab, `Plan`, following the `Races` precedent field-for-field:** `PLAN_HEADERS = ['ID','Date','Disc','Title','Intensity','DurationMin','Key','Notes','Phase','CreatedAt']`, `getPlanSheet_()` mirroring `getRacesSheet_()` including its trailing-column backfill, `listPlan_()` returning the same `{ok, plan}` envelope shape as `listRaces_()`, `handlePlanPost_()` routed off `body.kind === 'plan'` in `doPost`, `?plan=1` in `doGet`, ids generated client-side by `newPlanId()` so a delete never needs to read an id back out of an opaque no-cors response. `normalizePlanKey_()` accepts `TRUE`/`true`/`yes`/`y`/`1` because the tab is meant to stay hand-editable, exactly as `normalizeRaceStatus_()` does for "Locked in"/"locked"/"LOCKED". **`addBatch` and `deleteWeek` are load-bearing, not conveniences** — a week is ~10 sessions, and 10 concurrent no-cors POSTs race each other on `appendRow`; `addBatch` writes the lot in one `setValues()` block and copy-week issues exactly one request however many target weeks are ticked. `deletePlanWeek_()` walks the sheet **bottom-up**, because `deleteRow` shifts every row below it up by one and a top-down loop would skip rows and then delete the wrong ones — `findRaceRow_` never had to face this since a race delete only ever removes a single row. **No new nav tab.** The Plan view is `showView('plan')`, reached only from an "Edit plan →" link in This week, styled `dash-signal-link` to match the "See this month →" link Month is reached by. The nav is a fixed five buttons at 84px, and Month had already established that a full view can live without one — see the Navigation section, whose table had wrongly listed Month as a tab and omitted Warm up until this pass. **Which day's add-session form is open is module state (`planFormOpenFor`), never derived from the DOM** — same reason `raceFormOpen` exists, since `renderPlan()` fully rebuilds the view on a background Sheet refresh. Plan goes one step further than Races: the background re-render is *suppressed entirely* while a form is open, because keeping the form open isn't enough on its own — a rebuild would still discard whatever had been half-typed into it. Fresh data still lands in `plan` and `localStorage`; only the redraw waits, until the form is submitted or cancelled. **Two smaller things caught while building it:** the add-session form uses `aria-required` rather than the native `required` attribute, because `required` makes the browser block submission with its own transient bubble *before* the submit handler runs, which left the specific inline error messages unreachable — the same class of silent-block trap as the `type="url"` bug in decision 22; and `submitPlanToSheet` ends its fetch chain with `.catch(function(){})`, because `fetch()` rejects a promise on network failure rather than throwing synchronously, so the surrounding `try/catch` never sees it. `submitSessionToSheet` and `submitRaceToSheet` both still have that second issue — harmless (an unhandled rejection in the console, no user-visible effect) but worth fixing next time either is touched. **Deploy dependency — the same gate decision 22 documented for Races:** none of the sync half works until the updated `apps-script/Code.gs` is pasted into the Apps Script editor and redeployed (Deploy → Manage deployments → pencil → New version). Until then, plan entries still work locally — they appear immediately and survive a reload from the `triMomentumPlan_v1` localStorage cache — but silently fail to reach the Sheet and never appear on a second device, exactly like a session write against a stale deployment.
29. **The plan now surfaces in three places, and one field is permanently off-limits to all three.** Entering a plan (decision 28) only pays off if the app puts it in front of the athlete without them going looking for it, so Phase 2 added three surfaces and no fourth: the splash **Today card**, a **planned-session card** at the top of the check-in flow, and the already-present **"Edit plan →"** link in This week (`dash-signal-link`, matching "See this month →" — built in Phase 1, verified rather than rebuilt here). No new nav tab, for the same reason decision 28 gave.

    **⚠️ `a1` MUST NEVER BE PRE-FILLED. This is the single most important line in this decision.** `a1` ("Did it finish at the effort you intended?") is the sole input to the intensity-drift signal, and it is only worth anything as an *unprompted self-report*. The plan's `intensity` field is the intent set in advance; `a1` is whether the athlete actually held it. Seeding `a1` from the plan would make the app answer its own question — it would report near-zero drift forever, silently, with no error and no visible symptom, and the signal would be corrupted at its source with no way to tell from the stored data that anything was wrong. `applyPlanPrefill()` in `index.html` writes **only** `disc`, `intensity` and `duration`, and carries a block comment saying so; the same reasoning rules out `a2`, `a3`, `b2`, `b3` and `rpe`, all of which are outcome reports rather than restatements of a plan fact. If a future change makes the pre-fill "more helpful" by extending it, that change is wrong. **Every pre-filled value stays freely editable** — a pre-fill is a shortcut into the same form, never an assertion; the athlete can change `disc`/`intensity`/`duration` after tapping and the edited value is what `save()` writes.

    **The splash Today card's three states, and why each exists** (`renderSplashToday()`, `.splash-today`): *(a)* sessions planned today → one tappable row each, `{Disc} — {title}` over `{N} min · {intensity} · Key`; *(b)* no sessions today but the current Mon–Sun week has at least one planned → **"Rest day"**, because a planned rest is a real and useful answer that "nothing here" would misrepresent; *(c)* nothing planned anywhere in the week → the node is **removed from the DOM entirely**, not toggled with `[hidden]` the way `renderSplashCountdown()` hides the race card — an athlete who has never entered a plan should find no trace of the feature on the screen shown at every single load. The card is rebuilt from scratch on every call rather than diffed, so the init-time repaint after `refreshPlanFromSheet()` resolves is correct without extra bookkeeping. **Each session is its own `<button>`, not one tap target for the whole card** — with two sessions planned, a card-level tap would have to guess which one was just finished, and guessing wrong writes the wrong discipline into a check-in; the rest-day state renders as a plain `<p>` with no button at all, so it never offers an affordance that would do nothing. A tap by an athlete with no phase set is held in `pendingPlanPrefill` across the phase picker and re-applied in `choosePeriod()`, since Check-in is the one tab that can't bypass the picker.

    **Duration is bucketed, not copied.** The plan stores real minutes; the check-in asks for one of three buckets, whose option values are the **strings** `'30'`/`'60'`/`'120'` (that's what `pick()` writes and what the radios compare against, so `planDurationBucket()` must return a string, not a number). Boundaries follow the visible labels exactly — `≤45` → `'30'`, `46–90` → `'60'`, `>90` → `'120'` — and dropping into a short bucket deletes any stale `a3`, mirroring the rule `pick()` already applies. After a pre-fill the flow advances to the **next unanswered page** (`goToNextUnanswered()`) rather than re-rendering the page just tapped.

    **The in-flow card shows `notes` in the open, unlike the Plan view's own card**, which hides them behind a `<details>`: the HR cap, squad time or prescription is the reason the plan was typed in at all, and the check-in is exactly the moment it's wanted. The cards are scoped to whatever date is selected, so `dateField()`'s change handler now also calls `renderFlowPlanCards()` — rebuilt in place rather than via `render()`, which would recreate the date input and take focus off it mid-edit.

    **Two accessibility fixes came out of this, both real failures rather than propagated ones.** The global focus ring is `outline:3px solid var(--blue)` — invisible on the splash, whose background is *also* `--blue`; the Today card's buttons are the only focusable controls that have ever sat on it, and they override the ring to solid white (6.65:1 against the card). And contrast on the translucent splash cards has to be measured against the card's *composited* background, not `--blue`: at that background the existing `rgba(255,255,255,.7)` eyebrow measured **3.97:1** and the `.75` countdown unit suffix **4.35:1**, both under the 4.5:1 bar decision 24 set. Both were raised to `.82` (4.93:1) alongside the new card, so the two splash cards stay identical rather than the new one being quietly more compliant than the old. Also fixed in passing: `.flag` in `TRI-Momentum-Design-Guide.html` was `display:inline-flex`, which turned every inline `<span class="mono">` inside a callout into a flex item and broke the sentence into columns — now `inline-block` with the dot carrying its own spacing.

30. **Adherence became objective: it now scores the plan against the log, and it does so *alongside* the self-reported version rather than replacing it.** Phase 0 shipped Adherence reading `a2` ("Did you do it as planned?") — a real signal, but one scored against an intent the athlete reported in the same breath as the outcome, and structurally blind to the case that matters most: a session that was planned and simply never happened. Every existing tally opens with `if(!d.session){ return; }`, so a day with no logged session contributes nothing to any of them. That day is the entire point of this signal, which is why `tallyDays()` could not be reused and `tallyPlanDays()` exists instead, iterating the *union* of planned items and logged entries per day.

    **The load-bearing decision here is Stage A/Stage B coexistence, not the matcher.** The switch is per *period*, and in Month per *week*: `daysHavePlan(days)` — does any plan row fall in range? If yes, Stage B (objective, plan-anchored). If no, the Phase 0 Stage A path runs **exactly as it shipped**, untouched. This was the easy thing to get wrong: replacing a working signal with a "no plan entered for this week" message would have been a straight regression for every athlete who has never opened the Plan view, and would have made the plan feature a precondition for a signal that never needed one. A month can legitimately contain both kinds of week, and each is now reported the most precise way its own data allows. The visible tells that Stage B is running are the sub-label ("planned vs logged" rather than "by session"), the count label ("missed" rather than "didn't happen"), and an n-text that reads "3 of 4 planned · 1 unplanned" rather than "3 sessions".

    **Partially-entered weeks are computed honestly against whatever rows exist — no completeness threshold.** This is this phase's resolution of the spec's open question 1. A week with 3 of a real 10 sessions entered reports on those 3, and says nothing about the 7 it was never told about. The alternatives were a per-week "plan complete" flag (another thing to remember to set, and silently wrong when forgotten) or a heuristic minimum session count (arbitrary, and wrong for anyone with a genuinely light week). Both add a way for the signal to be quietly incorrect; neither fixes the underlying fact that the app cannot know what it wasn't told. The honest reading is the defensible one — the signal reports on the plan it has, and the athlete knows what they typed.

    **Matching is greedy one-to-one by `(date, disc)`, and its limitation is a known, accepted one.** Each planned session consumes at most one logged entry; each logged entry satisfies at most one planned session; walked in array order. Two planned and two logged swims on one day therefore produce **two scored results**, not one match plus a false miss plus a false extra — decision 27 already established that same-day, same-discipline doubles are real in this app, so that is precisely the case the rule exists for, and it is the case most likely to be silently wrong, so it was tested directly rather than reasoned about. The limitation: with two planned swims of *different* durations on one day, which logged entry pairs with which planned session is arbitrary, so the good/mid split between them can be the wrong way round. **The count is right; the attribution may not be.** An exact `planId` stamped on the check-in would remove the heuristic entirely, and is deliberately deferred — it needs a new column on the session sheet and a widening of `listSessions_`'s read range past column 13, which is a Code.gs change and a redeploy for a precision gain that has not yet proven necessary in practice.

    **A missed `key: true` session forces the period's severity to `bad`, and that rule is genuinely load-bearing rather than merely stored.** `missed_key_session` sits at priority 5, outranking every other rule in the set, so one missed key session headlines the week no matter how clean the rest of it was — verified against a week where three of four planned sessions completed and the severity still came back `bad`. That is the whole purpose of the flag: missing an easy commute run is noise, missing the week's VO2 set is the signal, and a rule set that counts them equally would average the one thing worth knowing into invisibility. Stage B extends the shared facts vocabulary with `missedKey` and `extraCount` rather than forking the rule set — the compound-pair, isolated-miss and clean-week rules are the same objects fed either kind of facts, and the three Stage-B-only rules simply never match in Stage A because those fields are undefined there.

    **"Not due yet" is a third outcome, and adding it was not optional.** A strict reading of the spec scores any planned session with no matching log entry as `bad`. That would mean every week reads as failing from Monday morning onward, since Friday's session hasn't happened yet on Tuesday. Only a planned session whose day has already *passed* can be scored bad; today and every future day resolve to `pending`, which stays out of the good/mid/bad tally entirely and draws as its own outlined grid state. **An unplanned logged session is `extra`, counted and reported but never scored bad** — an athlete who did more than they planned has not failed, and a signal that told them otherwise would be actively misleading.

    **`monthWeeks()` had a real bug, not a cosmetic one.** It only ever included weeks with at least one *logged* session, so a week that was fully planned and completely missed — the single most informative week this signal exists to report on — was silently absent from Month entirely, because a missed week has no logged entries by definition. It now also includes weeks with planned rows in range. A plan-only week has no logged sessions to read a phase off either, so the phase pill falls back to the phase its *planned* rows carry before falling back to the global once-ever setting — otherwise every missed week would display the athlete's current phase rather than the one it was planned in.

    **The grid's `missed` treatment, and the contrast check this project has now shipped wrong twice.** `.dash-cell-seg.missed` is a 1.5px inset `--dash-bad` ring over a 135° hatch of the same colour at 42%; `.planned` ("not due yet") is a 1px inset `--ink3` ring on transparent; `.extra` is a solid `--ink3` fill — grey, never red. The split between `missed` (hatched, no logged entry at all) and `bad` (solid `--dash-bad`, logged but reported as not done) is deliberate: they are different facts and the grid says so. **No new colour token was introduced** — `--dash-bad` and `--ink3` already carried exactly the right semantics, so there was nothing new to mirror into the design guide's hardcoded palette copy, which is a better outcome than inventing a token to satisfy the letter of the rule. Contrast was **measured numerically before shipping**, per decisions 24 and 26: the hatch is decorative and deliberately carries none of the burden, while the solid rings clear the 3:1 non-text bar at `--dash-bad` **5.74:1 on `--s1` / 4.82:1 on `--s4`** and `--ink3` **5.25:1 / 4.41:1**. **Found and deliberately not fixed:** `--dash-mid` `#E0A100` measures **2.27:1 on `--s1` and 1.90:1 on `--s4`**, i.e. the existing `mid` segment has never cleared 3:1 on *any* signal — already recorded as an open item in `style-guide.md`'s Accessibility section, pre-existing rather than introduced here, and left alone because darkening it recolours every grid, tri-bar, legend swatch and count row in the app at once and belongs in its own deliberate pass rather than smuggled into a feature change.

    **The legend is now signal-aware, which fixed a bug that predated this phase.** It previously hardcoded the drift vocabulary ("on plan / slightly off / well off") and rendered under all three subtabs, so it had been describing Adherence wrongly ever since Phase 0 added the third tab. Stage A now renders five items in the captioned signal's own words; Stage B renders seven. Each swatch is styled by applying the *actual* `.dash-cell-seg` modifier class it describes rather than a copied colour, so a legend entry can never drift from the thing it captions. Seven items is well past what the row was designed for, so it was checked: at 320px they wrap to three rows, 101px tall, with no horizontal overflow anywhere in the view.

    **This phase superseded an interrupted first attempt, whose partial code was audited rather than trusted wholesale.** A prior run was stopped after writing code but before any verification or documentation, leaving Phase-3-shaped functions in `index.html` that had never been run. Nothing had been committed between phases, so there was no clean diff separating that draft from verified Phase 1/2 work. **Kept, after verification:** the greedy matcher, `tallyPlanDays()`, the Stage A/B switch, the `pending` state, the `monthWeeks()` fix, the grid and legend treatments, and the duration bucket-parity rule — all confirmed correct in a real browser rather than by reading. **Kept but justified in writing:** `PLAN_MATCH_CACHE`, a `WeakMap` memo keyed on the day object, which is safe only because `buildWeek()` constructs brand-new day objects on every call and nothing mutates one in place — an invariant now written next to the cache, with instructions to delete it rather than try to invalidate it if that ever stops being true. **Fixed:** the `missed_key_session` headline built broken punctuation whenever the session had a title, rendering "Your key session, — VO2 5x3min — the run on Wednesday" — the em-dashes and the comma were both trying to set the clause off. **Proved rather than assumed:** the draft had refactored `computeSignalFacts()` to delegate to a new `accumulateFacts()`, which put the already-shipped Intensity-drift and Fuelling signals at risk. Comparing it against the pre-Phase-0 version in git (`git show HEAD:index.html` — HEAD predates all of this) confirmed it is a strict extract-method: same skip conditions, same iteration order, same field mapping, no semantic change. Both signals were then re-verified in the browser regardless.

31. **The plan became editable in place, and it now shows up inside Review — "This week's plan", a 7-day listing under the Adherence subtab.** Two changes that arrived together: a real `update` capability on a planned session, and a place to reach it from.

    **`update` is a genuine fifth plan action, and what it deliberately cannot touch is the important part.** `apps-script/Code.gs` gains `action:'update'` beside `add`/`addBatch`/`delete`/`deleteWeek`, locating the row with the existing `findPlanRow_()` and overwriting it in place. It writes **exactly seven columns — `Disc`, `Title`, `Intensity`, `DurationMin`, `Key`, `Notes`, `Phase`** — via a new `PLAN_EDITABLE_COL`/`PLAN_EDITABLE_COUNT` pair, so `ID`, `Date` and `CreatedAt` are outside the written range **by construction rather than by a guard a later edit could quietly drop**. `planRow_()` was refactored to build its full row from the same `planEditableCells_()` helper, so a full-row insert and an in-place edit cannot disagree about column order. **Date is not editable, and that is a product decision, not an oversight:** moving a session to another day is the drag-and-drop calendar editor this feature has excluded since decision 28 — reached through a different door, but the same thing — so delete-and-re-add stays the way to change a date. `CreatedAt` is excluded so it keeps meaning "when this row was first entered", and `phase` is preserved client-side (`updatePlanSession()` never assigns it) so retyping a session's duration months later doesn't silently restamp it with whatever phase is current now. The update branch takes the document lock, which `delete` does not: it is a read-then-blind-write (find the row index, then `setValues` into it), so a `deleteWeek` landing in between would shift rows up and overwrite a *different* session with this one's fields — a losing race that leaves no trace, unlike a lost delete.

    **One form, two modes — extended, not forked.** `renderPlanForm(root, dateISO, onCancel, editItem)` takes an optional item; with one, it pre-fills `disc`/`title`/`intensity`/`duration`/`key`/`notes`, relabels its submit button "Save changes", and routes to `updatePlanSession()` instead of `addPlanSession()`. Forking it would have let the validation rules, the `aria-required` error surface and the field set drift between adding and editing. `updatePlanSession()` **mutates the existing object rather than rebuilding it**, so any field it doesn't name carries over by construction — a field added to a plan item later can't be silently dropped by an edit. One subtlety worth keeping: in edit mode the radio-group fallbacks are the *item's own current values*, not the form defaults, so a session whose intensity was hand-typed into the Sheet as something outside the option list round-trips unchanged instead of being rewritten to `easy` by an edit that never touched that field.

    **Both entry points open the same form, through one function.** `openPlanDayForm(dateISO, editItem)` is the only way in; `goToPlanDay()` and `openPlanEditor()` are one-line wrappers. It sets `state.planWeek` to the day's Monday, sets `planFormOpenFor` (the same module state the Plan view's own "+ Add session" button uses — not a second mechanism), sets the new `planEditingId`, then renders and scrolls the day into view. **`planEditingId` is held alongside `planFormOpenFor`, never derived from it**, for the reason `planFormOpenFor` isn't derived from the DOM: `renderPlan()` rebuilds everything, including on a background Sheet refresh, and a half-edited form must not silently revert to an add. It's only ever non-null while `planFormOpenFor` is set, so the existing re-render guard in `refreshVisibleViewFromSheet()` already covers it; it's cleared on cancel, on save, on `stepPlanWeek`, and on `clearPlanWeek`. **The Plan view's own cards got the Edit action too** (`.plan-item-edit`, beside the existing Remove) — without it the only route to editing would have been the round trip through Review, which is where the cards *aren't*. The card foot went `flex-end` → `space-between` so the safe action and the destructive one sit at opposite ends rather than adjacent.

    **The Review listing is read-only on purpose, and it does not reuse `planItemCard()`.** That card carries a Remove button whose handler calls `renderPlan()`, which draws into the *hidden* Plan view — pressed from Review, the session would vanish from the data while staying on screen. Every mutation stays in the Plan view; the Review section only links there. Layout is compact rows: a fixed 52px date column (weekday + day-of-month, in the app's existing uppercase-tracked `--ink3` label style), stacked session lines, and a text-button affordance on the right. **A multi-session day stacks its lines under one date label rather than repeating the date.** Text comes from a new `planSummaryLine()` built *on top of* the existing `planMetaLine()` rather than re-listing the fields, so this row can never word the same facts differently from the splash and check-in cards.

    **The Edit affordance sits on the session, not on the day — a deliberate departure from "one Edit per row".** A day can hold more than one planned session (decision 27 established same-day, same-discipline doubles are real here), and decision 29 already refused a day-level tap target for exactly this reason on the splash Today card: with two sessions, one button has to guess which, and guessing wrong here silently overwrites the wrong session's fields. So each session line carries its own Edit, each with a distinct `aria-label`; a day with nothing planned shows "Nothing planned" and an **Add**. The labels differ because the destinations differ, and neither oversells: this is real field editing now, but it is still not date editing.

    **"Nothing planned", not "Rest day"** — the splash Today card's "Rest day" asserts something about *today* specifically; in a full seven-row listing, absence per row is self-evident and needs no claim. **The section is independent of the Stage A/B switch above it**, because it's *access to* the week's plan, not a reading of it: a week with no plan at all still renders seven rows, each with an Add, which is the fastest route from "I've never used this" to a first entered session. **Scoped to This week only — Month's Adherence is untouched**, verified rather than assumed; a per-week 7-day listing across a month's week blocks is a different and much larger layout problem.

    **Accessibility, and one real punctuation defect caught by looking at the output.** The section is a `<section>` with its own `aria-label`, because `.dash-section`'s eyebrow is a CSS `::before` on `attr(data-label)` — generated content, not a heading. Days and sessions are real lists; every affordance is a real `<button type="button">` with a distinct accessible name ("Edit" alone is ambiguous seven rows deep); the visible date label is abbreviated ("MON 10") but carries the unabbreviated date as an `aria-label`; the discipline dot is `aria-hidden` because the discipline is named in words beside it. Contrast was **measured on the rendered page**, per decisions 24/26: date label and "Nothing planned" `--ink3` **4.95:1**, summary text `--ink` **17.77:1**, Edit/Add `--blue` **7.40:1**, sub-line `--ink2` **7.12:1**, Key pill **6.03:1** — all past 4.5:1, with no new colour token introduced. Every tap target measures ≥44×44px at 320px, and keyboard activation was exercised, not assumed. **The defect:** `planSummaryLine()` originally joined discipline to title with an em-dash, which rendered "Swim — Squad — 8×100 CSS" whenever the title contained one — and titles containing one are the *common* case here, since the add-session form's own placeholder is "Squad — 8×100 CSS". The separator now degrades to "·" when the title already has an em-dash. Same collision decision 30 fixed in the missed-key-session headline.

    **Also in this pass, unrelated to the plan work: `--hero-accent`.** Warm up's yellow `#F8C44F` was a literal hex repeated in four rules. It's now a token, and the shared `.race-hero` component — used by *both* the Races view and the Plan view, so this is one change reaching two screens — takes it for its eyebrow and headline accent in place of `--blue-m`. The Races hero's **stat colours were deliberately left alone**: green/tangerine there encode commitment status (locked in vs considering), and flattening them to the accent would delete information rather than restyle it. Contrast measured before shipping: `#F8C44F` reaches **11.67:1** on `--ink` (the `.race-hero` background) and **9.41:1** on Warm up's `#182542` — comfortably past 4.5:1 for the 11px eyebrows and 3:1 for the 23/25px headline accents, and in fact better on its new background than on its original one.

    **Deploy dependency — the same gate as decisions 22 and 28.** Editing `apps-script/Code.gs` is not enough: it must be pasted into script.google.com and redeployed (Deploy → Manage deployments → pencil → New version) before an edit reaches the Sheet. Until that happens, edits still apply locally and survive a reload from the `triMomentumPlan_v1` cache, but never reach the Sheet and never appear on a second device — the same silent-failure mode as a session write against a stale deployment.

32. **Check-in now opens *on today's planned session* when there is one — a new landing screen, with "Log an unplanned session" as a named, first-class escape hatch.** Decision 29 put planned-session cards at the *top* of Check-in's first page: a hint sitting above the real question. That framing had the default backwards. If the athlete planned a session today, the honest opening question is "did you do the one you planned?", not a blank date-and-discipline form. This makes that the default and demotes the generic form to a deliberate choice.

    **A fourth flow mode, `state.mode === 'plan'`, rather than a separate view.** The landing lives inside the existing check-in state machine (`ask` | `plan` | `review` | `saved`), so `render()`, `renderChrome()` and `renderFoot()` stay the single places that decide what the flow looks like. The trap this created is that all three had an `else` branch that meant "ask" — a new mode falling through would have drawn the landing under a "Just finished · 1 / 5" progress header. Each got an explicit `plan` branch first: eyebrow "Today", **no step counter and a 0% bar** (the landing sits *before* step 1, so any counter would misstate where the athlete is), and the sticky Back/Next pair hidden outright rather than disabled — a greyed "Next" on a screen with no next step reads as breakage.

    **Three defects were caught by reading the code before any test ran, and all three were real.** *(a)* `els.foot.hidden = true` **does nothing on its own** here: `.flow-foot` sets `display:flex`, which beats the UA stylesheet's `[hidden]{display:none}`. This file has no global `[hidden]` rule — every hidden component carries its own (`.splash[hidden]`, `.plan-form-wrap[hidden]`, …) for exactly this reason, and `.flow-foot[hidden]{display:none}` was missing. *(b)* Routing the return-from-phase-picker through the new landing broke **decision 19**: the in-flow "Change phase" link is a detour out of a check-in already in progress and must return to the question you were on, but it shares `phaseReturnView === 'flow'` with the on-the-way-in phase gate. A separate `phaseReturnWantsLanding` flag now distinguishes them — set true by the Check-in nav tab, false by `openPhasePicker()` and by the splash-card path. *(c)* the `state.mode` comment still listed three modes.

    **The landing only appears when the flow is *pristine*, and that guard is the load-bearing one.** `flowIsPristine()` checks that `state.ans` holds nothing but its seeded `date`, that `note` is empty, and that the mode isn't `review`/`saved`. Without it, tapping away to Review and back — which has always resumed exactly where you were — would have silently discarded half-entered answers to show a landing screen. That would be a far worse regression than the screen is an improvement. A day with **no** planned session is untouched by all of this: `startCheckIn()` falls straight through to the previous behaviour, because there is nothing to frame around.

    **`startCheckIn()` is the single decision point.** The nav tab and the return-from-phase-gate both call it, so they cannot diverge. It also has to call `render()` explicitly: `showView('flow')` only refreshes the chrome, so without that the header would announce the landing while the previous screen's questions were still on the page. The splash Today-card tap (`startCheckInFromPlan`) deliberately does **not** route here — that tap has already answered the landing's question.

    **"Log an unplanned session" is a named design element, not an oversight.** Training off-plan is normal, and the app already models it: decision 30's `extra` category counts a logged session with no matching plan row and **never scores it `bad`**. The button drops into the manual flow exactly as it behaved before any plan integration — fresh answers, dated today, step 0, nothing pre-filled. **Decision 29's per-date cards are deliberately still shown** on the date page afterwards: they are a hint in that position, and suppressing them would break the date-change behaviour (`renderFlowPlanCards()` from `dateField()`'s change handler) that must keep working.

    **`a1` is still never pre-filled, through every path** — the rule from decision 29 is unchanged and was re-asserted rather than assumed. Both card entry points go through `usePlannedSession()` → `applyPlanPrefill()`, the single a1-guarded function; the unplanned path pre-fills nothing at all. Verified by driving the real UI in both directions across every page (a one-directional walk misses pages, since a pre-fill lands on the first *unanswered* page — `b3`, index 1 — leaving `date`/`disc` behind it and `duration`/`intensity` ahead), and end-to-end by logging an unplanned swim against a planned bike and confirming it renders `extra`, not `bad`, with `a1` carrying the athlete's own answer.

    **Correction pass — three defects found by an independent `mc-reviewer` over decisions 31–32, not by self-review.** Worth recording as such: the same work had already been documented above as verified, and these three survived that. All three are cases where the code did something reasonable-looking and told the athlete nothing.

    *(a) `updatePlanSession()` failed silently when the row was gone.* It opened `if(!item){ return null; }`, and the form's submit handler ignored the return — so an edit to a session deleted on another device (or one that only ever existed in a `localStorage` cache written before `Code.gs` was redeployed) left the athlete's typing sitting in a form that would never save, with no error anywhere. It now surfaces the reason and takes the dead form away. The message is held in module state (`planNotice = {date, message}`) rather than written into the form's own error node, because **the form is the thing being removed** — an error rendered inside it would be destroyed by the same `renderPlan()` that closes it. `planDayRow()` renders it using the *same* `.plan-form-error` class and `role="alert"` the form uses, so there is one error surface with two hosts rather than a second pattern. It is deliberately **not** cleared by a render (a background Sheet refresh must not wipe it before it is read) but by the next plan action — opening a form, changing week, or any mutation.

    *(b) The Check-in landing could offer to re-log a session already logged.* It rendered today's planned sessions unconditionally. Two planned, one logged at 7am, reopen at 6pm: both cards read "Check in on this →" identically, and tapping the wrong one wrote a duplicate — which the greedy matcher then reported as an **`extra`/unplanned** entry, corrupting the exact signal the screen exists to feed. `todayPlanSplit()` now splits today into still-to-log and already-logged **by calling `planDayResults()`**, the matcher Adherence itself runs on, never a second rule that could disagree with it. It is keyed on `res.entry`, not `res.cat`: a session logged as `didnt` scores `bad` but still has a real row, and re-logging it duplicates it exactly as a `good` one would. It builds its day object from `currentWeekDays()` rather than by hand, so what the matcher is handed is identical in shape to every other caller's — and brand-new per call, which is the invariant `PLAN_MATCH_CACHE` names as its safety condition. Already-logged sessions are **shown, not hidden**, after the ones still to log: the athlete gets the whole day, and the reason a card isn't offered is on the card rather than silently absent. `flowPlanCard(item, null)` renders it as a static `<div>` marked "Logged" — a div, not a disabled button, because there is no action to re-enable and a disabled control invites a hunt for the thing that would turn it back on. **When every planned session is already logged the landing does not appear at all** (`startCheckIn()` gates on `.open.length`), falling through to the manual flow exactly as before — a screen listing only things you cannot do is worse than no screen.

    *(c) `startCheckIn()` discarded a date the athlete had already chosen.* `flowIsPristine()` excluded `date` from its check, so an athlete who changed the date to log a missed session, tabbed away, and came back had `state.ans` reset to `{date: todayISO()}` — silently. Fixed in `flowIsPristine()` rather than at the reset (the reviewer's option (a)), which is safe because `startCheckIn()` is its **only** caller, and correct because a deliberately changed date genuinely *is* an entered answer: it is skipped by the key loop only because `state.ans` is *seeded* with today's. Preserving the date at the reset instead would have been worse — the landing screen lists *today's* plan, so showing it over a date three days ago is incoherent.

    Also removed here: a stale comment above `openPlanDayForm()` still asserting plan sync had "add and delete only… nothing here modifies a stored session", contradicted by decision 31 and by that function now being the entry point to in-place editing.

    **Verified in headless Chrome over http, against the committed baseline** — 66 checks, all passing. The harness instruments the real `index.html` (and `git show HEAD:index.html` for the baseline) with a generated `window.__t` handle at request time, so no test hook lives in the shipped file and the tests cannot drift from it. Served over http rather than `file://` on purpose: a `file://` origin makes `localStorage` and the service-worker check throw, which buries any real page error in environment noise. Contrast measured before shipping per decisions 24/26 — the logged card's "Logged" marker (`--done` on `--s3`) **5.54:1**, its title (`--ink2`) **6.76:1**, its meta (`--ink3`) **4.69:1**, and the day-hosted error notice **4.98:1**, all clear of 4.5:1. No new colour token was introduced.

33. **The big-percentage + tri-colour stat bar was removed from Month, for Fuelling and Adherence only.** A visual change, but with one structural trap worth recording: `renderStatBar()` has **two** call sites, and only one is obvious. The first was a month-wide aggregate at the top of `renderTrends()`'s Fuelling branch; the second is inside `renderSignalBody()`, which is **shared** — it draws This week's single card *and* every week block in Month, for all three signals. Deleting the line from the shared function would have silently stripped the bar from This week too, which was not what was asked. It is therefore suppressed by an `opts.hideStatBar` flag threaded from `renderMonthSection()`, set only by Month's Fuelling and Adherence sections. `monthTally()` went with the aggregate, since that was its only caller — it was Stage-A-only (`tallyDays` over a `catFn`) and never understood the plan-aware Stage B path, so it was not reusable for Adherence anyway.

    **Verification turned up something worth knowing about the bar's reachability.** It is gated on `t.total >= STAT_MIN_N` (8), but Stage A's `tallyDays()` counts at most **one session per day** (`d.session`) over a 7-day week — so under Stage A the per-week bar can never reach the threshold at all. The only places it was ever reachable are Stage-B Adherence (`tallyPlanDays()` counts per planned item, so a week can exceed 7) and the month-wide Fuelling aggregate just removed. Selector-counting therefore proved nothing, and the check that actually bites is a **DOM diff against `git show HEAD:index.html`**: This week × Intensity, This week × Fuelling and Month × Intensity render byte-identically to HEAD; This week × Adherence keeps a byte-identical stat bar (its wider DOM legitimately differs, since decision 31's day-list lives there); Month × Fuelling and Month × Adherence each lose the bar and **nothing else** — week-block count unchanged, and the remaining markup equal to the baseline with only the `.dash-stat-big`/`.tri-bar` pair excised.

    **One card component, two placements.** `flowPlanCard(item, ctaText)` was extracted from `renderFlowPlanCards()` and is used by both the in-flow hint list and the landing; only the call-to-action wording differs ("Use these details →" vs "Check in on this →"), so the two can never drift into showing different fields for the same session. On the landing the cards take a `--blue-m` border and more padding, because there they *are* the primary action rather than a shortcut above the real question. **Each session is its own card with its own action** — the same rule decisions 27, 29 and 31 all landed on, for the same reason: with two sessions planned, one shared action has to guess which, and guessing wrong pre-fills the wrong discipline.

34. **The splash Today card stopped offering already-logged sessions — the same defect as decision 32(b), on the sibling surface, fixed by reusing `todayPlanSplit()` rather than writing a second matcher.** Decision 32's correction pass fixed the Check-in landing, which had been rendering already-logged planned sessions with the same live "Check in on this →" as unlogged ones. The splash Today card (decision 29) had the identical defect and was not covered by that pass: it built its rows straight from `planItemsFor(today)` with no reference to the log at all, so tapping a session already logged that morning started a fresh pre-filled check-in and wrote a duplicate. **The consequence is signal corruption, not a cosmetic repeat** — the greedy matcher then has two logged entries for one planned session, and the spare is reported as `extra`, i.e. an unplanned session, in the very signal the plan feature was built to make objective. Reproduced end-to-end against `git show HEAD:index.html` before the fix: one duplicate written, `planDayResults()` returning `extras: 1`.

    **Found by the same independent `mc-reviewer` review that produced decision 32's three corrections** — recorded as such because it is the second time that review has caught something a self-review over the same work had already signed off as verified, and because it is a *class* of defect (a plan surface that offers an action without asking the log whether it's still available) rather than a one-off.

    **`todayPlanSplit()` is called, not re-implemented, and that is the whole point.** It already defers to `planDayResults()` — the matcher Adherence itself runs — so the card's idea of "already logged" cannot drift from the signal's. A second local rule would have been easy to write and would have been wrong the first time the two disagreed. It also inherits the two subtleties the landing fix established for free: the split is keyed on `res.entry`, not `res.cat` (a session logged as `didnt` scores `bad` but still has a real row, and re-logging it duplicates it exactly as a `good` one would), and the day object it matches against is built by `currentWeekDays()` rather than by hand, so it is brand-new per call — the invariant `PLAN_MATCH_CACHE` names as its safety condition.

    **The card's three existing states are untouched, and the guard for that is arithmetic rather than care.** `open.length + logged.length` is exactly `planItemsFor(today).length`, because `planDayResults()` builds its results from that same list, so the rest-day branch changed from `!items.length` to `!open.length && !logged.length` and triggers on precisely the same condition as before. "Rest day" still means "the week has a plan but today has nothing in it" and can never come to mean "today is planned and already done" — verified directly, including the case where the week's *other* planned day is the one that's logged.

    **Where this deliberately diverges from the Check-in landing: an all-logged day still renders the card.** The landing screen skips itself entirely when nothing is left to log, and correctly so — that screen exists to offer an action, and a screen listing only things you cannot do is worse than no screen. The splash card is also an *informational* surface: decision 29 already established that by treating "Rest day" as a real answer worth showing rather than an empty state. So it keeps telling the athlete what today was; it just stops inviting them to re-log it. The still-hidden state remains the one decision 29 defined — **no plan anywhere this week** — which is a different condition (no plan) from this one (planned and done), and the two must not be conflated.

    **Copy, structure and ordering are shared with `flowPlanCard()`; the CSS could not be.** A logged row is a `<div>`, not a disabled `<button>` — mirroring `flowPlanCard()`'s own rule for its own reason: there is no action here to re-enable, and a disabled control invites a hunt for the thing that would turn it back on. Open rows come first, logged after, same as the landing. But the *visual* treatment is genuinely unshareable: `flowPlanCard`'s `.is-logged` is light-on-light (`--s3` fill, `--ink2` text) and this card sits on the splash's `--blue` field, so the two have no colours in common. `splashTodayRow(item, isLogged)` was extracted so the open and logged rows can't drift in which fields they show. **No new colour token:** the dimmed title and the "Logged" marker both use `rgba(255,255,255,.82)`, the value the card's own eyebrow and meta line already carry — measured on the rendered page at **4.93:1** against the card's composited background (`rgba(255,255,255,.12)` over `--blue`), per decisions 24/26 and the `.82` rule in `style-guide.md`. Measured at the same time: open-row title 6.65:1, open-row meta 4.93:1, logged meta 4.93:1, open-row tap target 46px tall at 320px, no horizontal overflow, and the logged `<div>` confirmed absent from the tab order with exactly one focusable control left in the card.

    **The card now reads the log as well as the plan, and that new dependency needed two repaint paths, not one.** At init it was only repainted when `refreshPlanFromSheet()` resolved; `refreshLogFromSheet()` now gets its own `.then(renderSplashToday)` too, since a session logged on another device would otherwise keep being offered here. More importantly, `refreshVisibleViewFromSheet()` — the `visibilitychange` handler that exists because an installed PWA resumes from a frozen background instead of re-booting — **had no splash branch at all**, so a phone left sitting on the splash never re-checked anything. That was cosmetic while the card only read the plan; it is not now, because a stale card re-opens this exact duplicate-entry defect. A splash branch was added, guarded on the splash still being visible when the fetch resolves. Both repaint paths are idempotent — `renderSplashToday()` removes and rebuilds its own node.

    **Verified in headless Chrome over http against the committed baseline — 42 checks, all passing on the fix, 25/42 at HEAD**, so every assertion added here demonstrably discriminates rather than passing vacuously. The harness instruments the real `index.html` (and `git show HEAD:index.html` for the baseline) with a generated `window.__t` handle at request time, so no test hook lives in the shipped file. Covered: one planned and logged (static, marked, and clicking it neither dismisses the splash nor seeds a check-in); two planned with one logged (open first and still tappable, logged second and inert, exactly one live control); none logged (unchanged); `didnt` counted as logged; a logged *run* not consuming a planned *swim*; both rest-day regressions; the no-plan-this-week state asserted by **non-existence** of the node; the full a1 invariant (`a1` and every other outcome field still unset after a pre-fill, `disc`/`intensity`/`duration` still set, duration still the bucketed *string*); and end-to-end, logging the remaining open session produces two log rows, two good/mid/bad results and **zero `extra`**. The pre-existing unhandled rejection from `submitSessionToSheet` (already noted in decision 28 as worth fixing next time it's touched) is asserted present at both HEAD and fix rather than silenced, so it can't be mistaken for something this change introduced.

    **Found and deliberately not fixed:** `splashTodayName()` joins discipline to title with an em-dash, so a title that already contains one renders "Swim — Squad — 8×100 CSS" — the identical collision decision 31 fixed in `planSummaryLine()` and decision 30 fixed in the missed-key-session headline, and the common case given the add-session form's own placeholder is "Squad — 8×100 CSS". It is pre-existing at HEAD and affects three surfaces at once (splash card, in-flow hint cards, Check-in landing) because all three route through this one function, so the one-line separator fix would edit the Check-in landing's rendered text — out of scope here, and worth its own pass rather than being smuggled into a defect fix.

1. **The current app is the live product** — `index.html` plus its two Strength sub-apps (`TRI-swim-strength mobile app/`, `TRI-run-strength mobile app/`), all three co-deployed via GitHub Pages and reachable through the same 5-tab nav. The Strength apps are core architecture, not an optional add-on — treat changes to them with the same weight as changes to `index.html`. `frontend/`/`backend`/`supabase/` are the intended future direction, not yet active. Don't confuse "documented in this file" with "shipped."
2. **Freemium gate (target): Base phase only.** Free users get all 4 Base block weeks, all 6 pillars. Build, Peak, Recovery, and the race-proximity overlay require Pro. Paywall hits at the natural phase boundary — the highest-intent upgrade moment. Never show locked/blurred content. **Not implemented today — current app has no accounts or paywall.**
3. **Auth (target):** Supabase magic link + Google OAuth. No passwords. **Not implemented today.**
4. **Pricing (target):** $9.99 AUD/month, $79 AUD/year.
5. **Target stack:** React + Vite + Hono + Supabase + Stripe + Vercel/Fly.io. **Current app is a static HTML file with no backend, deployed via GitHub Pages.**
6. **Tips are intended to be static content bundled client-side** — not served from the database. Currently unused; the current app has no tip/prompt content system at all.
7. **Garmin integration is backend-only** (target) — credentials never touch the frontend.
8. **Club features are in scope but out of sequence** (target) — design auth and database with multi-user in mind, but do not build club UI until the last session.
9. **PWA, not native app** — true for both current and target.
10. **Target content is block-relative with variable block length.** The primary key would be `CARD_CONTENT[phase][phasePosition][pillar][day]`, resolved at runtime via `resolvePosition()`. This is unbuilt and unused; the current app instead asks phase once, manually, with no date-based resolution.
11. **Race-proximity overlay (target) is a distinct content layer**, independent of `tips.js`, keyed off days-to-race. Unbuilt — the current app never captures a race date.
12. **Sync strategy today is Google Sheets, not Supabase — and the Sheet is the source of truth, not `localStorage`.** `apps-script/Code.gs` handles both directions: `doPost` appends a row per logged session (fire-and-forget write), `doGet(?list=1)` returns the full log as JSON. `index.html` fetches from `doGet` on load and whenever This week/Month is opened, so a session logged on the phone shows up on the laptop; `localStorage` is only a same-device cache used when a fetch fails. This is a deliberate current-state choice, separate from the target's Supabase sync plan — revisit when/if the target architecture is activated.
13. **The target Supabase schema adopts the current app's check-in + computed-signals model, not the original reflection model.** `session_checkins` (mirroring `index.html`'s `log` array: `disc`, `duration`, `intensity`, `rpe`, `a1-a3`, `b2-b3`, `note`, `phase`) replaces the old `session_logs` + `week_reflections` tables. Signals (fuelling, intensity drift, compound-pair detection) stay computed at read time, not stored — matching how the current app already works. Freemium copy referencing "reflection persistence" or "readiness/grey zone" now means this model's equivalents. `calendar_sessions`/`week_cadences` are **dropped** — proactive day-of-week planning that neither the current app nor the adopted model implements; design fresh if it's ever actually wanted. **Still open:** whether the target UI adopts the current app's tabs (Strength/Check-in/This week/Month) or the original ones (Today/Week/Tips/Calendar) — see Session build order.
14. **`TRI-swim-strength mobile app/` and `TRI-run-strength mobile app/` are core architecture, not an add-on.** They implement the Strength tab's Swim and Ride/Run sub-tabs (via `<iframe>` in `index.html`), ship in this repo, and deploy under the same GitHub Pages site as the main app. They're separate deployable units (their own `index.html`/`app.js`/`manifest.webmanifest`/`sw.js`) for build-isolation reasons, not because they're peripheral — any session touching the Strength tab should treat these two directories with the same weight as `index.html` itself.
15. **The app opens on a splash screen, not straight into a tab.** `--blue` background, `favicon.png` logo, sub head, page-title-styled headline, base nav visible beneath it. No auto-advance — dismissed only by a nav tap. Tapping Check-in with no phase set routes to the training-phase picker first (see Training phase); every other tab still bypasses it, as before.
16. **`--blue` is now the favicon's blue, `#0022FF` — not `#1650C8`.** Rebranded so the brand blue and the app-icon artwork are the same colour everywhere it's defined: `index.html`, `style-guide.md`, and `TRI-Momentum-Design-Guide.html`. `--swim` (which has always shared `--blue`'s value) moved with it. `--blue-l`/`--blue-m` (tints) were left as-is — close enough in hue not to need re-deriving. Not touched: the base nav's hardcoded `#002DFF` and the PWA manifest's inline `theme_color` (`#002DFF`) — both pre-existing, separately-flagged discrepancies from `--blue`, unrelated to this change.
17. **Completion in Warm up and both Strength mini-apps is auto-detected only — there's no manual "mark complete" button anywhere anymore.** Checking the *last* exercise (or last round) off directly auto-fires a "Well done! [X] complete 🎉" banner immediately — mirroring TRI-run-strength's original `handleAllComplete()`, which TRI-swim-strength has now also gained (it previously had no auto-detect at all; its manual button was its only way to log a session, so the auto-detect logic was ported in *before* that button was removed, so it wasn't left with no way to complete). The banner increments an all-time completed-count (`localStorage`, device-local — not Sheet-synced), leaves the checklist checked rather than clearing it, logs exactly once per completion, and only its "Reset & go again" button or the always-visible header reset ("Reset warm-up" / "Reset session") clears it. Warm up's version is recoloured to the app's own `--done`/`--done-l` green and `--blue` button, matching the rest of `index.html`; both Strength apps keep their own existing yellow/green palette. **Consequence:** partial-completion logging (banking progress on a session you didn't finish) is no longer possible in any of the three — completion is all-or-nothing now.
18. **The documented type scale was wrong and has been corrected to match the shipped CSS.** Page titles are **23px** (not 26px) and eyebrows are **14px** (not 9–10px) — both now read directly from `index.html`'s `h1`/`.eyebrow` rules rather than the unused `frontend/src/index.css` scaffold they'd drifted to match instead. The splash headline (decision 15) was built against the old, wrong 26px figure and has been corrected to 23px/line-height 1.25 along with it.
19. **Check-in shows and lets you change the current phase, not just the once-ever picker.** A returning user who already has a phase saved skips straight into Check-in (see Training phase above) — with no way to see or change it without leaving to the dashboard first. The flow header now shows the current phase label plus a "Change phase" link on every question and on the review screen (hidden on the post-save screen, where the eyebrow already shows the phase). Tapping it opens the same one-time picker and returns straight back to the question you were on.
20. **Grouped/long check-in pages now clear the sticky footer at full scroll.** `.flow-body`'s bottom padding was 1.25rem — far short of the combined sticky-footer + tab-bar stack (~163px), so the last option on multi-question pages (e.g. "Did it finish at the effort you intended?" on the duration/intensity/rpe/a1 page) sat unreachable behind the footer. Padding raised to 6rem and `.q-set + .q-set` spacing raised to 5rem so every grouped page — including the review screen and the note field — clears with room to spare. Also restored the "Moderate / tempo" intensity option, which had been dropped from the `intensity` question's `opts` list.
21. **Month-view week cards give the training phase its own coloured pill, not plain inline text.** Previously "Week 31 · Peak" was one plain-text string, same weight/colour as everything else — easy to miss when scanning past weeks. `monthWeeks()` now returns structured `weekNum`/`phaseKey`/`isCurrent` fields instead of one joined string, and `monthWeekHeader()` renders a solid-colour pill next to the week number: green for Base/Taper, tangerine for Build (both Build 1 and Build 2 share the one colour), red for Peak. This introduces dedicated `--phase-{base,build,peak}` tokens rather than reusing discipline colours — it superseded an earlier documented plan (`--base: var(--swim)` etc., i.e. base=blue/build=green/peak=rust/taper=purple) that was never actually implemented in `index.html`; the shipped mapping instead is base=green, build=tangerine (a new colour, not borrowed from any discipline), peak=rust. Revisit if a distinct Taper treatment is ever wanted, since Base/Taper is still one combined phase (see Training phase) and currently shares Base's green.
22. **Added a Race calendar tab (5th, far-right) plus a race countdown on the splash.** New "Races" nav tab lets you add/remove races (name, date, commitment, optional website/notes) synced via a second sheet tab (`Races`, auto-created) using the same fire-and-forget-write/`cors`-read pattern as session logging — see `handleRacePost_`/`listRaces_` in `apps-script/Code.gs` and the RACE CALENDAR SYNC block in `index.html`. The race `id` is generated client-side so a delete never needs to read an id back out of an opaque no-cors add response. The splash's blue header now shows a translucent countdown card ("NEXT RACE · [name] · N weeks · N days to go") for whichever saved race is soonest — auto-selected by nearest date, not a manually-flagged "goal race"; hidden entirely (not shown empty) when no race is saved. Each race also carries a **commitment pill** — "Considering" (`--modified`, amber, default) or "Locked in" (`--done`, green) — set via a two-option toggle on the Add a race form, shown on every race card, mirroring the confirmed-vs-tentative colour language from the athlete's own race-planning spreadsheet this feature was built from. **Nav order:** Strength moved to 2nd, between Warm up and Check-in (Warm up, Strength, Check-in, This week, Races) so the three session-logging tabs sit together ahead of the two review/planning tabs. **Deploy dependency:** this needs the updated `apps-script/Code.gs` pasted into the Apps Script editor and redeployed (Deploy → Manage deployments → pencil → New version) before races will actually sync — until then, adds/deletes still work locally (localStorage cache) but silently fail to reach the Sheet, exactly like a session log write against a stale deployment. **Bug caught during build:** the website field must be `type="text"`, not `type="url"` — the `url` input type demands a full `https://` scheme, so a bare `ironman.com/geelong` fails native constraint validation and silently blocks the whole form's submission with no visible error. The scheme is prefixed at render time instead (see `raceCard` in `index.html`). **Collapsed by default, matching the athlete's BalanceTRI club-calendar reference app:** the add-race form is hidden behind a full-width "Add a race +" pill button (`.race-add-toggle`) that flips to a solid-white "Cancel" (`--s1` fill, `--bd2` border — **not transparent**: a transparent fill on the page's `--bg` background read as a disabled/unpressable button, so it's a real secondary button instead) when open, and auto-collapses back after a successful add; each race card is itself a native `<details>` disclosure, collapsed by default, with date/countdown/name/commitment pill always visible in the `<summary>` even collapsed (so the days/weeks-to-go figure never needs an extra tap) and notes/website-link/remove tucked behind the expand. Open/closed state for the add-race toggle is tracked in a module-level `raceFormOpen` flag rather than derived from the DOM, since `renderRaces()` fully rebuilds the view (including on a background Sheet refresh) and a mid-fill-in form shouldn't silently snap shut because of that. **Month grouping:** the Upcoming and Past lists now group consecutive same-month races under a "Sep 2026"-style label (`.race-month-label`, `groupRacesByMonth()` in `index.html`), echoing the reference app's month-grouped calendar view. **Splash copy:** the sub head ("Helping triathletes progress") was removed entirely and the headline changed from "Mobility, intensity, fuelling, strength" to "Helping you prepare for your next race," styled identically (page-title spec) — the splash now leads with the race-countdown framing this feature added, rather than the original mobility/intensity/fuelling/strength positioning. **Form field label fix:** the Commitment field's label is a `<span>` (it labels a radio group, not one input) rather than a `<label>`, so the original `.race-field label.field-lbl` CSS selector never matched it — broadened to `.race-field .field-lbl` so both label types render identically, and doubled the label-to-input gap from 6px to 12px. That gap is also now real: `raceField()` used to nest the input inside the `<label>` after a `<br>`, which most browsers don't apply vertical margin around, so the label's margin-bottom had no visible effect — label and input are now siblings instead. **Race website, labelled:** an expanded race card's website now shows under a "RACE WEBSITE" section label (`.race-card-section-label`, echoing the reference app's labelled expanded-card sections) rather than a generic "Race website" link label. **Leveraged the Strength apps' own component language, not just the BalanceTRI reference:** race cards dropped their flat `--bd2` border in favour of the Strength apps' "lifted card" soft two-layer shadow (`0 1px 2px rgba(14,16,32,.04), 0 10px 24px -10px rgba(14,16,32,.16)`, recoloured from their ink to this app's own `--ink` rather than reusing their palette or their Outfit font), and the "is-next" emphasis became a `--blue`-tinted shadow + `--blue-l` fill instead of a border-colour swap — the same idea as the Strength apps' checked/active exercise-row shadows. The Upcoming/Past section headers got their own eyebrow-plus-trailing-count-pill treatment (`.race-section`/`.race-section-hd`, e.g. "Upcoming — 4 races"), echoing the Strength apps' phase-card header (eyebrow + status pill) — built as its own class rather than modifying the shared `.dash-section` (used by This week/Month), so the change stays scoped to Races.

---

35. **Check-in auto-progresses instead of requiring a "Next" tap after every answer.** Previously, answering a question — even on a multi-question page like `['duration','intensity','rpe','a1']` — only updated the progress bar; the athlete still had to tap Next to move anywhere. `pick()` now schedules an auto-advance (`scheduleAutoProgress()`) after each answer: if the current page has another unanswered question, focus/scroll moves to it (`focusQuestion()`, mirroring `focusHeading()`'s pattern but mid-page); once every question on the page is answered, it advances exactly as tapping Next would (`go(state.step + 1, true)`), including the final page falling through to Review. **Debounced (450ms), not immediate** — a radio group fires `change` on every arrow-key press while a keyboard user is still choosing between options, so an immediate jump would fling them onward mid-navigation before they'd settled on an answer. `go()` clears the pending timer first, so a manual Back/Next/review-row tap can never race a still-pending auto-advance into a double-move. Every question heading (not just the page's first, `h1`/`step-h`) now gets a stable `id` (`step-h-{key}`) and `tabIndex=-1` so it can be a focus target; `.q-set h2:focus{outline:none}` mirrors the existing `h1:focus{outline:none}` rule so a screen-reader-only focus move doesn't draw a visible ring around a heading nobody clicked. The date field is deliberately excluded from triggering auto-progress (it always carries a default value, so it's "answered" from load, and re-editing it — e.g. to log a missed session — must not fling the athlete forward again). Verified in a live browser walkthrough: single-question pages (`b3`) advance straight through; the four-question page scrolls to each unanswered heading in turn (duration → intensity → rpe → a1) and only changes page after `a1`; the fuelling page (`a3`, shown only on long sessions) follows immediately after, confirming the `onlyLong` filter in `steps()` is unaffected. No new colour token; no style-guide.md change, since this is interaction timing, not a new visual pattern.

---

36. **The splash Today card gained a fourth, independent section — "Earlier this week" — a day-at-a-time carousel over still-open planned sessions from earlier days in the current week, each tapping into the identical check-in flow a Today row uses.** Previously the splash only ever surfaced *today's* plan; a missed Tuesday swim had no route back onto the splash at all — the athlete had to know to go looking for it via Review's "This week's plan" or the Plan view itself. This closes that gap at the one screen every session already opens on. Went through an intermediate flat-list version first (never shipped — caught before it was committed) before landing on the carousel below, once it was clear a day can hold more than one missed session and a flat list buries that.

    **Reused the machinery decisions 30/32(b)/34 already built, rather than writing a new "is this logged?" test.** `todayPlanSplit()` — which defers to `planDayResults()`, the same greedy matcher the Adherence signal runs on — is now `dayPlanSplit(dateISO)`, generalised from today-only to any date in the current week (`todayPlanSplit()` survives as a one-line wrapper, so every existing caller — the Check-in landing, `startCheckIn()` — is untouched). `splashPrevDays()` walks `currentWeekDays()` for every day before today, keeps each day whose `.open` list (from `dayPlanSplit`) is non-empty as `{date, items}`, then reverses the result — `currentWeekDays()` is Monday-first, and there's no direct "walk it backward" primitive, so building forward and reversing is simpler than a second iteration order. A hand-rolled "any log entry on that date" check was deliberately avoided: it is exactly the class of defect decisions 32(b) and 34 both had to fix on the other two plan surfaces (splash Today card, Check-in landing) — a second rule that could disagree with the matcher about what's already logged, and disagreement there means a tap writes a duplicate that the matcher then reports as an unplanned `extra`, corrupting the very signal the plan feature exists to make objective.

    **Ordered most-recent-missed-day first, oldest last — on request, not the original Monday-first draft.** The day before today is the freshest miss and the one worth seeing without paging, so it's index `0`; the right arrow ("Earlier day") pages backwards in time from there, the left arrow ("More recent day") pages back toward today. `splashPrevDayIndex` is module state, not derived from the DOM — the same reason `planFormOpenFor`/`raceFormOpen` are: `renderSplashToday()` rebuilds this node from scratch on every call (a background Sheet refresh, every prev/next tap), and the index is re-clamped against the current day list on every render in `splashPrevCarousel()`, since that list can shrink (a session gets logged) or grow between renders.

    **One day showing at a time, not a flat list — the load-bearing reason for the carousel over the original flat design.** A day can hold more than one still-open session (verified live against a real Sheet day carrying three), and a flat list of every open session across the week buries that under whichever day sorts first. The nav row — two 44×44px circular `‹`/`›` buttons either side of a day label (`.splash-prev-nav-btn`, same white 3px focus-ring override as `.splash-today-btn`) — is skipped entirely when there's only one missed day, since two permanently-dimmed dead buttons would be worse than no nav row; a button past the end of its direction dims (`.is-disabled`, opacity `.35`) rather than disappearing, with **no click handler attached at all** rather than a disabled attribute, so the row never jumps around as you page. A row of dots (`.splash-prev-dots`) tracks position when there's more than one day. Below the nav, the current day's sessions reuse `splashTodayRow(item, false)` verbatim — no per-row date label any more, since the nav already says which day, once. Paging announces via the existing `say()` live-region pattern (`stepPlanWeek()`'s own convention) rather than manually re-focusing a button the rebuild just destroyed.

    **Independent of Today's own state, not nested under it.** Today's three states (list / "Rest day" / card removed entirely) are unchanged; "Earlier this week" appends below whichever one rendered, separated by the same `14px`/`rgba(255,255,255,.22)` top-border divider `.splash-today-item + .splash-today-item` already uses between rows. This had to be a real change to `renderSplashToday()`'s control flow: the Rest-day branch used to `return` immediately after appending the card, which would have made a rest day today silently swallow an earlier-week miss. A rest day today says nothing about whether Monday's session ever got logged — verified by seeding a Monday-only plan on a today with nothing planned and confirming both sections render together.

    **`startCheckInFromPlan(item)` now dates the check-in from `item.date`, not a hardcoded `todayISO()`** — the one behavioural change reaching outside the splash. For a Today row `item.date` already equals today, so this is a strict generalisation with no behaviour change on the existing path (confirmed by re-testing a Today-row tap after the change). The same fix applies to the `pendingPlanPrefill` branch in `choosePeriod()` (the path a tap takes when no phase is set yet and the athlete is routed through the phase picker first) — it now also seeds `state.ans.date` from the held item rather than assuming today, so a picker detour can't silently move a missed session's log entry onto today's date.

    **Verified live against the real production Sheet, not synthetic seed data** — the Apps Script deployment fix earlier the same day meant a fresh page load pulled real plan/log rows, which surfaced a real multi-session day (three sessions on one Saturday) that confirmed the carousel's reason for existing rather than just its happy path. Paged forward through six real missed days and back, confirmed the near-end button dims and the far-end one is live, confirmed dots track position, and confirmed tapping a session mid-carousel opens Check-in with the date field showing that day (not today), `disc` pre-filled, and `a1`/every other outcome field untouched — the same a1-never-prefilled invariant decision 29 established, unchanged by this pass.

    **Day label is `.splash-prev-day-label`** — abbreviated weekday + day-of-month ("MON 17"), the same abbreviation `dashPlanDay()` (decision 31's Review listing) already uses, with the full weekday date in an `aria-label` for screen readers rather than the abbreviated text. Uses the same `rgba(255,255,255,.82)` already verified at 4.93:1 against the card's composited background (decisions 24/29) — no new colour token. `style-guide.md` and `TRI-Momentum-Design-Guide.html` both updated with the carousel (`.splash-prev`/`splashPrevDays()`/`splashPrevCarousel()`), including a rendered "Earlier this week" tile in the design guide's Splash component mockup.

*Last updated: 2026-08-23. **The check-in note field's character limit doubled, from 140 to 280** (`index.html`'s `noteBlock()`) — a small, standalone change, unrelated to the splash work below. **Also this update: the live Apps Script deployment was found to be running the wrong code entirely** — every read (`?list=1`/`?plan=1`/`?races=1`) was returning identical, unrelated placeholder data (fields like `clubFocus` that exist nowhere in this repo's `apps-script/Code.gs`), which explained why a week of real check-ins weren't reaching the Sheet: writes were almost certainly hitting the same stale/wrong script. Diagnosed by fetching the deployed `/exec` URL directly and comparing the response shape against `listSessions_()`/`listPlan_()`/`listRaces_()` in the committed `Code.gs` — same class of gap decisions 22/28/31 already flag as a standing risk ("edits to `Code.gs` need a manual redeploy"), just more severe than a missed redeploy of a recent change. Fixed by the athlete redeploying from the Apps Script editor; confirmed fixed by re-fetching all three read endpoints and seeing them return real, distinct, correctly-shaped data, then loading the live app and seeing real plan data render on the splash. **Splash gained an "Earlier this week" section (decision 36)** — still-open planned sessions from earlier days in the current week now surface on the splash below Today as a day-at-a-time carousel (left/right arrows page between days, a day can hold more than one session), starting on the most recent missed day and paging backwards in time from there, each session tapping into the same check-in flow a Today row uses but dated from that session's own day. Built by generalising `todayPlanSplit()` into `dayPlanSplit(dateISO)` and reusing it, rather than writing a second "is this logged?" rule — the same discipline decisions 32(b)/34 established after the cost of not doing so. `startCheckInFromPlan()` now dates the check-in from the tapped session's own date rather than assuming today; verified this is a no-op on the existing Today-row path. An intermediate flat-list version (one row per missed session, no paging) was built and then replaced before ever being committed, once a real multi-session day made clear it needed grouping by day rather than a flat run. Verified live against the real production Sheet (post-redeploy): paged through six real missed days including one with three sessions, confirmed boundary dimming and dot position, and confirmed tapping a mid-carousel session opens Check-in dated for that day, not today, with `a1` and every other outcome field left untouched. Prior update (2026-08-19): **Check-in now auto-progresses (decision 35)** — answering a question advances to the next unanswered question on the page, or the next page once the page is complete, without a manual Next tap; debounced 450ms so keyboard radio-group navigation doesn't get flung forward mid-choice, and cancelled by any manual navigation. Verified live in-browser across a single-question page, a four-question page, and the page-to-page transition. Prior update (2026-08-16, latest same day). **Applied decision 32(b)'s fix to its sibling surface — the splash Today card no longer offers already-logged sessions (decision 34).** Same defect class, found by the same independent `mc-reviewer` review, on the one plan surface that pass didn't cover: `renderSplashToday()` built its rows straight from `planItemsFor(today)` with no reference to the log, so tapping a session already logged that morning wrote a duplicate — which the matcher then reported as an unplanned `extra`, corrupting the signal the plan feature exists to make objective. Fixed by **calling `todayPlanSplit()`**, the function the landing fix already built, rather than writing a second matcher: it defers to `planDayResults()` (the matcher Adherence itself runs), so the card's idea of "already logged" cannot drift from the signal's, and it inherits the `res.entry`-not-`res.cat` keying and the fresh-day-object rule for free. `splashTodayRow()` was extracted; logged rows render as static `<div>`s marked "Logged" after the open ones, mirroring `flowPlanCard()`'s copy, div-not-button rule and ordering — but not its CSS, which is light-on-light and unusable on the splash's `--blue` field. **Deliberate divergence from the landing:** an all-logged day still renders the card, because this surface is informational as well as an action prompt (decision 29 already treats "Rest day" that way); the still-hidden state remains "no plan anywhere this week", a different condition that must not be conflated with "planned and done". The card now reads the log as well as the plan, so it gained two repaint paths — an init `refreshLogFromSheet().then(...)`, and a **splash branch in `refreshVisibleViewFromSheet()`, which had none at all**: an installed PWA resumed from a frozen background never re-boots, and a stale card re-opens this exact defect. Verified in headless Chrome over http against `git show HEAD:index.html` — **42 checks passing on the fix, 25/42 at HEAD**, so no assertion passes vacuously; the baseline run reproduces the duplicate and its `extras: 1` end-to-end. No new colour token; contrast measured on the rendered page at 4.93:1 (logged title, meta and marker), tap targets 46px at 320px, logged rows confirmed out of the tab order. **Found and deliberately not fixed:** `splashTodayName()` has the same em-dash collision decision 31 fixed in `planSummaryLine()` ("Swim — Squad — 8×100 CSS"), pre-existing at HEAD and shared by three surfaces including the out-of-scope Check-in landing — worth its own pass. Prior update (same day): **A correction pass on decisions 31–32, driven by an independent `mc-reviewer` rather than by self-review** — worth noting for the record, since that same work was already documented above as verified and these three survived it. Folded into decision 32 rather than logged as new decisions. *(a)* `updatePlanSession()` returned `null` and said nothing when the row no longer existed, leaving an athlete's edits in a form that would never save; it now surfaces the reason and closes the form, with the message held in module state (`planNotice`) so it survives the very re-render that removes the form, rendered by the day row through the **same** `.plan-form-error`/`role="alert"` node rather than a second pattern. *(b)* The Check-in landing offered already-logged sessions with the same "Check in on this →" as unlogged ones — tapping one wrote a duplicate that the matcher then reported as an unplanned `extra`, corrupting the signal the screen exists to feed. `todayPlanSplit()` now reuses `planDayResults()` (the Adherence matcher itself, never a second rule) keyed on `res.entry` not `res.cat`, and shows logged sessions as static, untappable `.is-logged` cards after the ones still to log; with **everything** logged the landing doesn't appear at all and Check-in falls through to the manual flow. *(c)* `flowIsPristine()` excluded `date`, so `startCheckIn()` silently reset a date the athlete had chosen to log a missed session — fixed in `flowIsPristine()` itself, safe because `startCheckIn()` is its only caller and correct because a changed date genuinely is an entered answer. Also removed a stale comment claiming plan sync had no update action. **Also in this pass, unrelated:** removed the big-percentage + tri-bar stat component from Month for Fuelling and Adherence only (decision 33) — via an `opts.hideStatBar` flag, because `renderSignalBody()` is shared with This week and all three signals, so deleting the call would have stripped it from This week too; `monthTally()` went with the month-wide aggregate that was its only caller. Verification surfaced that the per-week bar is gated at `STAT_MIN_N` 8 while Stage A's `tallyDays()` counts at most one session per day over a 7-day week, so it was only ever reachable for Stage-B Adherence and the removed aggregate — which is why the check is a **rendered-DOM diff against `git show HEAD:index.html`** rather than a selector count. All of the above verified by 66 headless-Chrome checks over http (a `file://` origin makes `localStorage` and the service-worker check throw, burying real errors), instrumenting the real `index.html` at request time so no test hook lives in the shipped file. Contrast measured per decisions 24/26: "Logged" marker 5.54:1, card title 6.76:1, card meta 4.69:1, day-hosted notice 4.98:1 — no new colour token. Regression-confirmed: `a1` still pre-filled by nothing, decision 19's "Change phase returns you to the question you were on" still holds, and Adherence's good/mid/bad/extra/pending categories are unchanged. Prior update (same day): Redesigned Check-in's entry point around today's plan (decision 32) — when today has a planned session, Check-in now opens on a landing screen listing it (or all of them) with a per-session "Check in on this →", and a named **"Log an unplanned session"** escape hatch that drops into the pre-existing manual flow with nothing pre-filled. Implemented as a fourth flow mode (`state.mode === 'plan'`) inside the existing state machine rather than a separate view, which meant `render()`/`renderChrome()`/`renderFoot()` each needed an explicit branch — all three previously treated their `else` as "ask", so a new mode would have drawn the landing under a "1 / 5" progress header. **Three defects were caught by reading before any test ran:** `.flow-foot` sets `display:flex`, which beats the UA `[hidden]{display:none}`, so hiding the sticky footer silently did nothing until `.flow-foot[hidden]` was added (this file has no global `[hidden]` rule — every component carries its own); routing the phase-picker return through the landing broke **decision 19**'s "Change phase returns you to the question you were on", now separated by a `phaseReturnWantsLanding` flag; and the `state.mode` comment was stale. **The load-bearing guard is `flowIsPristine()`** — the landing only appears when nothing has been entered yet, so tapping away to Review and back still resumes an in-progress check-in instead of discarding it. A day with nothing planned is completely untouched. **`a1` is still never pre-filled through any path** (decision 29's rule), re-asserted rather than assumed: verified by walking the real UI in *both* directions, since a pre-fill lands on the first unanswered page (`b3`) and a one-way walk misses either `date`/`disc` behind it or `duration`/`intensity` ahead of it. Verified end-to-end that an unplanned session logged via the new button renders as `extra` in Adherence, never `bad`. `flowPlanCard()` was extracted so the landing and decision 29's in-flow hint cards are one component differing only in call-to-action. Prior update (same day): made the training plan editable in place and surfaced it inside Review (decision 31). Two things landed together: a real fifth plan action, `update`, in `apps-script/Code.gs`, and a "This week's plan" 7-day listing under This week's **Adherence** subtab that links into it. **The load-bearing detail of the update action is what it structurally cannot reach** — it writes exactly the `Disc`…`Phase` column block via `PLAN_EDITABLE_COL`/`PLAN_EDITABLE_COUNT`, so `ID`, `Date` and `CreatedAt` sit outside the written range by construction rather than behind a guard a later edit could drop. **Date is deliberately not editable**: moving a session to another day is the drag-and-drop calendar editor excluded since decision 28, reached through a different door, so delete-and-re-add stays the way — this narrows the old blanket "no editing past plans" exclusion to date only. `phase` is preserved rather than restamped, so editing an old session's duration doesn't relabel the phase it was planned in. The update branch takes the document lock (which `delete` does not) because it is a read-then-blind-write that a concurrent `deleteWeek` could point at the wrong row, silently. `renderPlanForm()` was **extended, not forked**, into an optional edit mode, and both entry points — the Plan view's own session cards, which gained an Edit beside Remove, and the new Review listing — go through one `openPlanDayForm()`. The Review listing is **read-only and deliberately does not reuse `planItemCard()`**, whose Remove handler calls `renderPlan()` into the hidden Plan view and would make a deleted session linger on screen. Its layout is compact rows: fixed date column, stacked session lines for multi-session days under a single date label, `planSummaryLine()` composed on top of the existing `planMetaLine()` so wording can't drift from the splash and check-in cards. **The Edit affordance sits per session, not per day** — decision 27 made same-day doubles real and decision 29 already refused a day-level tap target for the same reason: one button among two sessions has to guess, and guessing wrong here overwrites the wrong row's fields. Empty days read "Nothing planned" (not "Rest day", which is a claim about *today* specific to the splash) and offer Add. The section renders independently of the Stage A/B switch, since it is access to the plan rather than a reading of it, and is **scoped to This week — Month's Adherence is untouched and was verified so**. A real punctuation defect was caught by looking at rendered output rather than the diff: the summary joined discipline to title with an em-dash, rendering "Swim — Squad — 8×100 CSS" whenever the title contained one, which is the common case given the form's own placeholder — the separator now degrades to "·", the same collision decision 30 fixed in the missed-key-session headline. Contrast measured on the rendered page per decisions 24/26 (`--ink3` 4.95:1, `--ink` 17.77:1, `--blue` 7.40:1, `--ink2` 7.12:1, Key pill 6.03:1), all tap targets ≥44×44px at 320px, keyboard activation exercised. **Also in this pass, unrelated:** Warm up's yellow `#F8C44F` became a real token, `--hero-accent`, and the shared `.race-hero` component (used by *both* Races and Plan, so one change reaching two screens) took it for its eyebrow and headline accent in place of `--blue-m`; the Races hero's stat colours were left alone because green/tangerine there encode commitment status rather than decoration. Measured 11.67:1 on `--ink` and 9.41:1 on Warm up's navy. **Requires a manual redeploy of `apps-script/Code.gs`** before edits reach the Sheet — same gate as decisions 22 and 28. Prior update (2026-08-16): upgraded Adherence from self-reported to objective (decision 30) — the last phase of the training-plan feature. The signal now joins the entered plan against the log via a new `tallyPlanDays()`, which iterates the **union** of planned items and logged entries per day, because every existing tally opens with `if(!d.session){ return; }` and is therefore structurally blind to a planned session that never happened — the one case the signal exists for. **The load-bearing decision is Stage A/Stage B coexistence:** a period with any plan row in range computes objectively; a period with none falls back to the Phase 0 `a2` path exactly as it shipped, so an athlete who has never opened the Plan view sees no change at all rather than a "no plan entered" regression. Partially-entered weeks compute honestly against whatever rows exist, with no completeness threshold — this phase's resolution of the spec's open question 1. Matching is greedy one-to-one by `(date, disc)`, so two planned and two logged swims on one day produce two scored results rather than a false missed/extra pair (the count is right; with differing durations the attribution between them may not be — an exact `planId` is deliberately deferred, since it needs a Code.gs column and a redeploy). A missed `key:true` session sits at priority 5 and forces the period's severity to `bad` however clean the rest of the week was. Sessions whose day hasn't passed resolve to "not due yet" rather than counting as misses, and unplanned logged sessions are `extra`, never `bad`. Fixed a real bug in `monthWeeks()`, which only ever included weeks with a *logged* session and so silently hid exactly the fully-planned, fully-missed weeks this signal most needs to show. New grid states — hatched `missed`, outlined "not due yet", grey `extra` — **introduced no new colour token**, since `--dash-bad` and `--ink3` already carried the right semantics; contrast was measured numerically before shipping per decisions 24 and 26 (`--dash-bad` 5.74:1/4.82:1, `--ink3` 5.25:1/4.41:1 against `--s1`/`--s4`). Surfaced but deliberately left alone: `--dash-mid` has never cleared 3:1 on any signal (2.27:1/1.90:1) — pre-existing, already logged in `style-guide.md`, and a token-level change that warrants its own pass. The legend is now signal-aware, fixing a bug that predated this phase (it had described Adherence in the drift vocabulary since Phase 0). This phase **superseded an interrupted first attempt whose partial code was audited rather than trusted** — the draft's `computeSignalFacts()` refactor was proved a strict extract-method against `git show HEAD:index.html`, so Intensity-drift and Fuelling were confirmed unaffected, and a punctuation defect in the key-session headline was fixed. Prior update (2026-08-15): surfaced the training plan in three places (decision 29): a "Today" card on the splash beneath the race countdown (one tappable row per session planned today, "Rest day" when the week has a plan but today doesn't, and **removed from the DOM entirely** — a stricter hide than the countdown card's `[hidden]` — when the week has no plan at all), a planned-session card at the top of the check-in flow that pre-fills `disc`/`intensity`/`duration` and shows the session's notes in the open, and the "Edit plan →" link in This week that Phase 1 had already added (verified, not rebuilt). **The load-bearing rule in that decision is that `a1` must never be pre-filled** — it is the sole input to the intensity-drift signal and only means anything as an unprompted self-report, so `applyPlanPrefill()` writes only those three fields and every one of them stays freely editable afterwards. Planned minutes map to the check-in's three string buckets (`'30'`/`'60'`/`'120'`) at ≤45 / 46–90 / >90. Two genuine accessibility failures were fixed alongside it: the splash needed its own white focus ring (the global one is `--blue` on a `--blue` background, i.e. invisible), and the translucent splash cards' label text was raised from `rgba(255,255,255,.7)`/`.75` to `.82`, since against the cards' composited background the originals measured 3.97:1 and 4.35:1 — under the 4.5:1 bar decision 24 set. Both splash cards are now `width:100%` capped at 300px so they align. Also fixed `.flag` in the design guide, which was `inline-flex` and broke any callout containing an inline `<span class="mono">` into columns. Prior update (2026-08-14): updated the This week/Month "by session" grid (decision 27) so a cell renders one bar per logged session in that discipline that day, instead of collapsing to just the day's last-logged entry — a second same-day, same-discipline session (e.g. two bike sessions on one Thursday) no longer silently disappears from the grid. Prior update (2026-08-12): darkened `--phase-build` from `#D9660B` to `#A64C09` app-wide (decision 26) — the original only reached 3.23:1 contrast against `--phase-build-l`, under the 4.5:1 AA bar decision 24 established, and the same pairing was already failing on the Month week Build phase pill, not just the newly-recoloured "Considering" race pill that surfaced it. `--phase-base`/`--phase-peak` checked at the same time and were already compliant (5.46:1, 6.03:1). Prior update (2026-08-11): restructured the race card into a two-band header (decision 25) — a `--blue-l` banner holding the race name as a bold headline with the commitment pill inline next to it, and a `--s3` grey band directly below holding date + countdown — replacing the earlier flat stack, and closer to the reference club-calendar app's own race card layout. Recoloured the "Considering" pill to `--phase-build`/`--phase-build-l` (tangerine) — see decision 26 above for the contrast fix this triggered. Darkened `--ink3` from `#8B93B8` to `#626B8C` app-wide (decision 24) — the original failed WCAG AA contrast (3.02:1 on white, need 4.5:1) and was the app's primary label/eyebrow/date/hint colour, not just a Races-tab issue; fixed at the token level so every screen got corrected together, mirrored into the design guide's own copy of the palette, and documented as a standing rule in a new "Accessibility" section in `style-guide.md`. Also roughly doubled the race card's internal spacing (padding, and every gap between date/name/pill/notes/website/remove), which read as cramped in the first pass. Switched typography app-wide from DM Sans + DM Mono to a single family, Outfit (decision 23) — `--fd`/`--fb`/`--fm` all repoint to it, the eyebrow/label "mono" look now comes from case + tracking rather than a separate monospace font, and `TRI-Momentum-Design-Guide.html`'s self-contained base64-embedded fonts were swapped to match (four DM Sans/DM Mono `@font-face` blocks → one Outfit variable-font block), with its Type section rewritten since "two families, two jobs" no longer describes the system. Also dropped the "+" from the "Add a race" button label and switched race cards back to showing the full stored website URL rather than a parsed host+path. Added a Race calendar tab (decision 22) — 5th/far-right nav tab for adding/removing races, synced via a new `Races` sheet tab in the same Google Sheet (see `apps-script/Code.gs`), plus a race countdown card on the splash showing the nearest upcoming race and a commitment pill ("Considering"/"Locked in") on every race card. Reordered the bottom nav so Strength sits 2nd, between Warm up and Check-in, ahead of This week/Races. Redesigned the Races tab to match the athlete's BalanceTRI club-calendar reference app: the add-race form now hides behind an "Add a race" toggle button instead of always sitting open at the top, each race card collapses to a native `<details>` disclosure (date/countdown/name/pill always visible even collapsed; notes/website/remove revealed on tap) instead of always showing everything, the Upcoming/Past lists now group same-month races under a "Sep 2026"-style label, and an expanded card's website now shows under a "RACE WEBSITE" section label. Fixed the "Cancel" toggle button to use a solid white fill instead of transparent — transparent read as a disabled/unpressable button against the page's near-white grey background. Also pulled in the Strength apps' own component language: race cards now use a soft "lifted card" shadow instead of a flat border, the is-next race gets a blue-tinted shadow instead of a border-colour swap, and the Upcoming/Past headers gained a trailing count pill next to their eyebrow — all echoing the Strength apps' exercise-row and phase-card-header patterns, recoloured to this app's own tokens. Changed the splash headline to "Helping you prepare for your next race" and removed the sub head entirely. Fixed the Commitment field's label to use the same styling as every other field label (the CSS selector required a `<label>` tag, but Commitment's is a `<span>`) and doubled the label-to-input spacing on the add-race form — also fixed that spacing to actually render, since the old `<br>`-based layout nested the input inside the label where a margin-bottom has no visible effect. Caught and fixed a real bug along the way: the website field can't use `type="url"` (blocks submission on a bare domain with no scheme) — switched to `type="text"` with the scheme prefixed at render time. Updated the nav tab count references (4→5) throughout, and the "Base navigation"/added "Add a race toggle" and "Race card" tiles to `TRI-Momentum-Design-Guide.html` and `style-guide.md` to match. Requires a manual redeploy of the updated `apps-script/Code.gs` before race sync goes live — see decision 22. Prior update (same day): added a coloured phase pill to Month-view week cards (decision 21), replacing the plain-text "Week N · Phase" label — introduced dedicated `--phase-{base,build,peak}` tokens and retired the never-implemented `--base`/`--build`/`--peak`/`--taper`-map-to-discipline-colour plan from the Colour tokens block. Fixed grouped check-in pages clipping their last option behind the sticky footer at full scroll (decision 20) and restored the missing "Moderate / tempo" intensity option. See decision 14 and "Two codebases" intro for the current-vs-target split. Owner: Justin. Update this file whenever a decision changes, or when the target architecture moves from scaffold to active development.*
